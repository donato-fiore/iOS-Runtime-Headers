// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTC7SWIFTUIP33_3734FCB8B87024BD212C6F4B89BF01BE13VIEWCACHEITEM_H
#define _TTC7SWIFTUIP33_3734FCB8B87024BD212C6F4B89BF01BE13VIEWCACHEITEM_H

@class SwiftObject;



@interface _TtC7SwiftUIP33_3734FCB8B87024BD212C6F4B89BF01BE13ViewCacheItem : SwiftObject {
    ? cache;
    ? subgraph;
    ? outputs;
    ? _state;
    ? _list;
    ? elements;
    ? elementIndex;
    ? releaseElements;
    ? indirectMap;
    ? transition;
    ? transitionType;
    ? id;
    ? reuseIdentifier;
    ? section;
    ? zIndex;
    ? insertionTransactionID;
    ? animationCount;
    ? usedSeed;
    ? placementSeed;
    ? commitSeed;
    ? prefetchSeed;
    ? displayIndex;
    ? removedSeed;
    ? placement;
    ? releaseSecondaryElements;
    ? willEnableTransitions;
    ? willAnimateRemoval;
    ? parentingPhase;
    ? hasWarned;
}






@end


#endif