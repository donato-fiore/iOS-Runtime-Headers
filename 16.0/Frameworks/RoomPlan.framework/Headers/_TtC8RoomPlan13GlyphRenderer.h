// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTC8ROOMPLAN13GLYPHRENDERER_H
#define _TTC8ROOMPLAN13GLYPHRENDERER_H

@class SwiftObject;



@interface _TtC8RoomPlan13GlyphRenderer : SwiftObject {
    ? device;
    ? pixelFormat;
    ? sampleCount;
    ? defaultLibrary;
    ? layer;
    ? edgeFactor;
    ? insideFactor;
    ? state;
    ? time;
    ? _strokeWeight;
    ? rotationSpring;
    ? scaleSpring;
    ? translationSpring;
    ? cubeResolveSpring;
    ? planeResolveSpring;
    ? alphaSpring;
    ? rotation;
    ? angularVelocity;
    ? R;
    ? MVP;
    ? mvp_ortho;
    ? mvp_persp;
    ? computePipelineState;
    ? postTessellationPipelineState;
    ? msaaTex;
    ? splineData;
    ? dotsRenderer;
    ? deviceController;
    ? a_vel;
    ? dirty_tesselation_factors;
    ? lastDrawableSize;
}






@end


#endif