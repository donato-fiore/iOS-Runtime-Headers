// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTC15PROXIMITYREADER22PAYMENTCARDREADERACTOR_H
#define _TTC15PROXIMITYREADER22PAYMENTCARDREADERACTOR_H

@class SwiftObject;



@interface _TtC15ProximityReader22PaymentCardReaderActor : SwiftObject {
    ? $defaultActor;
    ? options;
    ? updateHandlers;
    ? releaseConnectionHandler;
    ? activeSession;
    ? activeSessionId;
    ? eventHandlers;
    ? eventsContinuations;
    ? arbiter;
    ? foregroundState;
    ? xpcDisconnectionExpected;
    ? connection;
    ? readerIdentifierCache;
    ? installId;
    ? readerCount;
    ? foregroundObserver;
}






@end


#endif