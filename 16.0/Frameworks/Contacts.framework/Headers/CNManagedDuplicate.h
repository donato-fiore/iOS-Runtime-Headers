// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef CNMANAGEDDUPLICATE_H
#define CNMANAGEDDUPLICATE_H


#import <Foundation/Foundation.h>


@interface CNManagedDuplicate : NSObject {
    ? signature;
    ? isAvailable;
    ? isIgnored;
    ? primaryID;
    ? managedCohorts;
    ? userSelectedContactImage;
    ? _contactStore;
    ? _privateContacts;
    ? _privateMergedContact;
    ? _mergedNameComponents;
}




-(id)init;


@end


#endif