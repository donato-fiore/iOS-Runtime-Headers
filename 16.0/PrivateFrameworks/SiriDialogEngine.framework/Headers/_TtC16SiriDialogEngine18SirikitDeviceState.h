// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTC16SIRIDIALOGENGINE18SIRIKITDEVICESTATE_H
#define _TTC16SIRIDIALOGENGINE18SIRIKITDEVICESTATE_H

@class SwiftObject;



@interface _TtC16SiriDialogEngine18SirikitDeviceState : SwiftObject {
    ? mockGlobals;
    ? isHomePod;
    ? isWatch;
    ? isMac;
    ? isAppleTV;
    ? isPhone;
    ? isPad;
    ? isCarPlay;
    ? isCarDnD;
    ? isSidekick;
    ? isTextToSpeechEnabled;
    ? isLockedWithPasscode;
    ? isAuthenticated;
    ? isEyesFree;
    ? isVoiceTriggerEnabled;
    ? isVOX;
    ? isNetworkConnected;
    ? interactionType;
    ? inputOrigin;
    ? multiUserState;
    ? voiceGender;
    ? userAssignedDeviceName;
    ? deviceName;
    ? companionName;
    ? siriLocale;
    ? deviceRegionCode;
    ? restrictions;
}






@end


#endif