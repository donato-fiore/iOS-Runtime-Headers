// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTC18PODCASTSFOUNDATION24PODCASTEPISODEATTRIBUTES_H
#define _TTC18PODCASTSFOUNDATION24PODCASTEPISODEATTRIBUTES_H

@class SwiftObject;



@interface _TtC18PodcastsFoundation24PodcastEpisodeAttributes : SwiftObject {
    ? artistName;
    ? url;
    ? subscribable;
    ? feedUrl;
    ? offers;
    ? name;
    ? itunesTitle;
    ? description;
    ? artwork;
    ? copyright;
    ? kind;
    ? fullDescription;
    ? releaseDateTime;
    ? contentRating;
    ? websiteUrl;
    ? genreNames;
    ? mediaKind;
    ? assetUrl;
    ? durationInMilliseconds;
    ? guid;
    ? episodeNumber;
    ? seasonNumber;
    ? _releaseDateCache;
}






@end


#endif