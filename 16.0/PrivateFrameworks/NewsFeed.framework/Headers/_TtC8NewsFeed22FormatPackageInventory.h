// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTC8NEWSFEED22FORMATPACKAGEINVENTORY_H
#define _TTC8NEWSFEED22FORMATPACKAGEINVENTORY_H

@class SwiftObject;



@interface _TtC8NewsFeed22FormatPackageInventory : SwiftObject {
    ? identifier;
    ? version;
    ? role;
    ? _direction;
    ? _name;
    ? _comment;
    ? _groups;
    ? _items;
    ? _snippets;
    ? _supplementaries;
    ? _styles;
    ? _slots;
    ? _types;
    ? _decorations;
    ? _options;
    ? _selectors;
    ? _localizations;
    ? columnSystem;
    ? _references;
    ? compilerOptions;
}






@end


#endif