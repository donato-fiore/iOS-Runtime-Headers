// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTC8NEWSFEED27FORMATLAYOUTVIEWPOOLMANAGER_H
#define _TTC8NEWSFEED27FORMATLAYOUTVIEWPOOLMANAGER_H

@class SwiftObject;



@interface _TtC8NewsFeed27FormatLayoutViewPoolManager : SwiftObject {
    ? viewPool;
    ? buttonPool;
    ? imagePool;
    ? labelPool;
    ? layoutViewPool;
    ? stateViewPool;
    ? layeredMediaViewPool;
    ? videoViewPool;
    ? micaViewPool;
    ? progressViewPool;
    ? shineViewPool;
    ? gradientViewPool;
    ? issueCoverViewWithImagePool;
    ? issueCoverViewWithLayeredMediaPool;
    ? canEnqueue;
}




-(void)didReceiveMemoryWarning;


@end


#endif