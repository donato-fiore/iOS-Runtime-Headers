// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTC7CHATKIT20POPEXPLOSIONRENDERER_H
#define _TTC7CHATKIT20POPEXPLOSIONRENDERER_H

@class SwiftObject;



@interface _TtC7ChatKit20PopExplosionRenderer : SwiftObject {
    ? parameters;
    ? bulge;
    ? device;
    ? bufferResourceOptions;
    ? computePipelineState;
    ? renderPipelineState;
    ? vertexCount;
    ? vertexBuffer;
    ? indexCount;
    ? indexBuffer;
    ? pieceCount;
    ? pieceBuffer;
    ? totalSize;
    ? dimensions;
    ? isPrepared;
}






@end


#endif