// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTC15FOCUSSETTINGSUI23ACTIVITYCONFIGVIEWMODEL_H
#define _TTC15FOCUSSETTINGSUI23ACTIVITYCONFIGVIEWMODEL_H

@class SwiftObject;



@interface _TtC15FocusSettingsUI23ActivityConfigViewModel : SwiftObject {
    ? activationManager;
    ? _configurationManager;
    ? _modeActive;
    ? focusAssertionListener;
    ? suggestedPagesClient;
    ? _identifier;
    ? meDevice;
    ? _focusConfigurationActions;
    ? focusConfigurationActionActor;
    ? _focusSystemConfigurationActions;
    ? focusSystemConfigurationActionActor;
    ? peopleWithExceptionsDirty;
    ? sortedPeopleWithExceptions;
    ? appsWithExceptionsDirty;
    ? sortedAppsWithExceptions;
    ? showSilenceListMessage;
    ? _customizedHomeScreenPagesEnabled;
    ? homeScreenSnapshots;
    ? homeScreenSnapshotsForSuggestedPages;
    ? homeScreenSnapshotsLoaded;
    ? postersController;
    ? postersDelegateHandler;
    ? postersService;
    ? lockScreenSnapshots;
    ? homeScreenWallpaperSnapshot;
    ? selectedWatchFace;
    ? watchFacesToList;
    ? notificationsHeaderAccessibilityIdentifier;
    ? notificationsFooterAccessibilityIdentifier;
    ? suggestionTitle;
    ? customizationHeader;
    ? homeScreenHeader;
    ? allContactsTitle;
    ? deleteActivityButtonTitle;
    ? deleteActivityConfirmation;
    ? allowAvailabilityStatusPrompt;
    ? availabilityStatusFooter;
}






@end


#endif