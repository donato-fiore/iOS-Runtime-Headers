// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTC22HOMEAUTOMATIONINTERNAL28HOMEAUTOMATIONMETRICSCONTEXT_H
#define _TTC22HOMEAUTOMATIONINTERNAL28HOMEAUTOMATIONMETRICSCONTEXT_H

@class SwiftObject;



@interface _TtC22HomeAutomationInternal28HomeAutomationMetricsContext : SwiftObject {
    ? version;
    ? taskType;
    ? hasCurrentHome;
    ? numberOfHomes;
    ? numberOfHomepods;
    ? numberOfATV;
    ? numberOfTargetedEntities;
    ? isSceneRequest;
    ? serviceTypes;
    ? targetedContainer;
    ? attribute;
    ? conditionType;
    ? isRecurring;
    ? completionType;
    ? commandOutcome;
    ? actionOutcome;
    ? commandCompletedEvents;
    ? customTaskId;
    ? customInteractionId;
    ? trialExperiments;
}






@end


#endif