// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTCC22HOMEAUTOMATIONINTERNAL13INTENTCONTEXT7BUILDER_H
#define _TTCC22HOMEAUTOMATIONINTERNAL13INTENTCONTEXT7BUILDER_H

@class SwiftObject;



@interface _TtCC22HomeAutomationInternal13IntentContext7Builder : SwiftObject {
    ? hasAllQuantifier;
    ? smallestUserMentionedContainerLayer;
    ? isRemoteHomeRequest;
    ? attribute;
    ? entityNames;
    ? roomNames;
    ? zoneNames;
    ? targetedHomeName;
    ? deviceTypes;
    ? targetState;
    ? isSensorTrippedRequest;
    ? isGarageDoorRequest;
    ? userDefinedDuration;
    ? isOpenRequest;
    ? isCloseRequest;
    ? isLockRequest;
    ? isUnlockRequest;
    ? isDisarmRequest;
    ? isTVRequest;
    ? numericValue;
    ? isInCurrentRoom;
    ? isColorRequest;
    ? isScheduleRequest;
    ? isSceneRequest;
    ? targetColor;
}






@end


#endif