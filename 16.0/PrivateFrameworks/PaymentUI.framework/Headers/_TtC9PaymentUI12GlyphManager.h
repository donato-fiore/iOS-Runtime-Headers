// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTC9PAYMENTUI12GLYPHMANAGER_H
#define _TTC9PAYMENTUI12GLYPHMANAGER_H

@class SwiftObject;



@interface _TtC9PaymentUI12GlyphManager : SwiftObject {
    ? internalState;
    ? deferringState;
    ? deferredStateRequiresRetry;
    ? biometricSuccessOutstanding;
    ? stateTransitionIndex;
    ? completionHandlers;
    ? deferredCompletionHandlers;
    ? userIntentStyle;
    ? authenticating;
    ? disablePasscodeFallback;
    ? presentPasscode;
    ? idleStateIsPasscode;
    ? matchFailed;
    ? matchFailedAtLeastOnce;
    ? lastFailureWasUnboundBiometric;
    ? glyphSymbol;
    ? glyphColorMode;
    ? physicalButtonEdge;
    ? physicalButtonHidden;
    ? glyphUpdaterCallback;
    ? aGlyph;
    ? avoidGlyphViewUpdates;
    ? hasPearl;
    ? context;
}






@end


#endif