// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTC9PAYMENTUI18AUTHENTICATORMODEL_H
#define _TTC9PAYMENTUI18AUTHENTICATORMODEL_H

@class SwiftObject;



@interface _TtC9PaymentUI18AuthenticatorModel : SwiftObject {
    ? _authenticating;
    ? _policy;
    ? _coachingState;
    ? _evaluationState;
    ? _bypassAuthenticator;
    ? _isInBioLockout;
    ? _disablePasscodeFallback;
    ? _footerViewType;
    ? _passcodeEntry;
    ? _externalPasswordEntry;
    ? _glyph;
    ? _isAuthenticationDetached;
    ? glyphManager;
    ? userIntentStyle;
    ? hasPearl;
    ? physicalButtonEdge;
    ? _isDetachedAuthenticationSheetPresented;
    ? physicalButtonHidden;
    ? context;
    ? externalizedContextCompletion;
}






@end


#endif