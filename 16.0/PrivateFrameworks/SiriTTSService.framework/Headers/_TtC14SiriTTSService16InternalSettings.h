// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTC14SIRITTSSERVICE16INTERNALSETTINGS_H
#define _TTC14SIRITTSSERVICE16INTERNALSETTINGS_H

@class SwiftObject;



@interface _TtC14SiriTTSService16InternalSettings : SwiftObject {
    ? _enableDiagnostic;
    ? _logSensitiveText;
    ? _disableCache;
    ? _disableAssetCleaning;
    ? _allowAnyAssetSubscriber;
    ? _defaultToNonDiscretionaryDownloads;
    ? _enableLocalVoices;
    ? _defaultVolume;
    ? _defaultPitch;
    ? _defaultRate;
    ? _whisper;
    ? _serverLogs;
    ? _serverTTSTimeout;
    ? _deviceTTSWaitTime;
    ? _disableDeviceRacing;
    ? _forceServerTTS;
    ? _disableServerTTS;
    ? _disableInlineStreamTTS;
    ? _disableOspreyStreaming;
    ? _streamBufferDuration;
    ? _useBetaVoice;
    ? _ospreyEndpointURL;
    ? _simulateNetworkStall;
    ? _disableDeviceNeuralTTS;
    ? _useSSMLInput;
    ? _disableMobileAssetURLReset;
    ? _ignorePowerAndThermalState;
    ? _disableAssetUpdate;
}






@end


#endif