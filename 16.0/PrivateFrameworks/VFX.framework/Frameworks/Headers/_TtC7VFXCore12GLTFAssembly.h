// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTC7VFXCORE12GLTFASSEMBLY_H
#define _TTC7VFXCORE12GLTFASSEMBLY_H

@class SwiftObject;



@interface _TtC7VFXCore12GLTFAssembly : SwiftObject {
    ? directoryPath;
    ? json;
    ? bin;
    ? nodes;
    ? cameras;
    ? materials;
    ? meshes;
    ? textures;
    ? samplers;
    ? accessors;
    ? textureWithSamplers;
    ? bufferViews;
    ? buffers;
    ? device;
    ? opaqueDepthStencilState;
    ? transparentDepthStencilState;
    ? renderer;
    ? entityManager;
}






@end


#endif