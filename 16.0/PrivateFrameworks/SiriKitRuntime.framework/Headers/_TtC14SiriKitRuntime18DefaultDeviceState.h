// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTC14SIRIKITRUNTIME18DEFAULTDEVICESTATE_H
#define _TTC14SIRIKITRUNTIME18DEFAULTDEVICESTATE_H

@class SwiftObject;



@interface _TtC14SiriKitRuntime18DefaultDeviceState : SwiftObject {
    ? inputOrigin;
    ? interactionType;
    ? multiUserState;
    ? siriLocale;
    ? siriVoiceGender;
    ? isEyesFree;
    ? isVoiceTriggerEnabled;
    ? isTextToSpeechEnabled;
    ? isDirectAction;
    ? renderDeviceIdiom;
    ? sessionHandOffContinuityID;
    ? preferencesLanguageCode;
    ? personalDomainsAuthenticationMode;
    ? bargeInContext;
    ? endpointId;
    ? restrictedCommands;
    ? authenticationState;
    ? peerName;
    ? isLockedWithPasscodeClosure;
    ? isHomePodGestalt;
    ? isAppleTVGestalt;
    ? isPhoneGestalt;
    ? isPadGestalt;
    ? isPodGestalt;
}






@end


#endif