// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTC17PROMOTEDCONTENTUI20APPSTOREDATAPROVIDER_H
#define _TTC17PROMOTEDCONTENTUI20APPSTOREDATAPROVIDER_H

@class SwiftObject;



@interface _TtC17PromotedContentUI20AppStoreDataProvider : SwiftObject {
    ? metadataExpirationTimer;
    ? storeFront;
    ? storeFrontLocale;
    ? theLock;
    ? subsequentRequestsDisabled;
    ? session;
    ? fetchingSession;
    ? lastFetch;
    ? searchAdsFacade;
    ? sessionPersistence;
    ? firstInitializeCall;
    ? greenTeaLogger;
    ? nextRequestContextId;
    ? initializedAt;
    ? previousGetAdAt;
    ? waitForClientInfo;
}






@end


#endif