// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTC17COREODIESSENTIALS7ODNATOR_H
#define _TTC17COREODIESSENTIALS7ODNATOR_H

@class SwiftObject;



@interface _TtC17CoreODIEssentials7ODNator : SwiftObject {
    ? inoWorkflowID;
    ? spWorkflowID;
    ? providerID;
    ? assessmentBaseURL;
    ? assessmentBaseURLORD;
    ? assessmentBaseURLOTD;
    ? profileManager;
    ? assessmentWebService;
    ? serviceName;
    ? supportsPrecomputation;
    ? externalGSToken;
    ? state;
    ? assessmentGUID;
    ? assessmentGUIDString;
    ? assessmentGUIDData;
    ? trainingGUID;
    ? trainingGUIDString;
    ? trainingGUIDData;
    ? bindings;
    ? inProcessSemaphore;
    ? sharingTimeSemaphore;
    ? useSendORD;
    ? useSendOTD;
    ? holdForSendingResults;
    ? ordRequest;
    ? otdRequest;
    ? profileId;
    ? clientGUIDFactory;
}






@end


#endif