// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTC12NEWSARTICLES27ARTICLEVIEWERVIEWCONTROLLER_H
#define _TTC12NEWSARTICLES27ARTICLEVIEWERVIEWCONTROLLER_H

@class UIViewController;



@interface _TtC12NewsArticles27ArticleViewerViewController : UIViewController {
    ? navigationItemStyle;
    ? observableScrollView;
    ? styler;
    ? eventHandler;
    ? pageViewController;
    ? pageBlueprintProvider;
    ? blueprintProvider;
    ? coverViewManager;
    ? eventManager;
    ? prewarm;
    ? barCompressionManager;
    ? articleViewerImpressionManager;
    ? interstitialAdManager;
    ? paidAccessChecker;
    ? featureManager;
    ? toolbarManager;
    ? featureTransitions;
    ? hasPresentedFirstPage;
    ? pageSheetPresentationObserver;
    ? commands;
    ? onPagePresented;
    ? state;
    ? isPagingEnabled;
    ? sceneProvider;
}




-(id)initWithCoder:(id)arg0 ;
-(id)initWithNibName:(id)arg0 bundle:(id)arg1 ;
-(void)smartInvertStatusDidChange;


@end


#endif