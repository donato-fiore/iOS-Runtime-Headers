// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTCV11SEYMOURCORE15SESSIONPROTOBUFP33_AA7F910130437AD35ECAF4F7ED8807FE13_STORAGECLASS_H
#define _TTCV11SEYMOURCORE15SESSIONPROTOBUFP33_AA7F910130437AD35ECAF4F7ED8807FE13_STORAGECLASS_H

@class SwiftObject;



@interface _TtCV11SeymourCore15SessionProtobufP33_AA7F910130437AD35ECAF4F7ED8807FE13_StorageClass : SwiftObject {
    ? _identifier;
    ? _catalogWorkout;
    ? _activityRings;
    ? _countdownIntent;
    ? _countdown;
    ? _energyMetric;
    ? _heartRateMetric;
    ? _playbackIntent;
    ? _playback;
    ? _termination;
    ? _workout;
    ? _scoreMetrics;
    ? _userMassMetric;
    ? _distanceMetric;
    ? _watchConnection;
    ? _gymKitConnection;
    ? _workoutActivityType;
    ? _hostAppBundleIdentifier;
    ? _displayPreferences;
    ? _unitPreferences;
    ? _supportedMetrics;
    ? _watchConnectionRejection;
    ? _remoteDisplayConnection;
    ? _paceMetric;
    ? _alternateMetric;
    ? _alternate;
    ? _multiUserSession;
    ? _multiUserSessionIdentities;
}






@end


#endif