// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTC7NEWSUI228TODAYFEEDGROUPEMITTERFACTORY_H
#define _TTC7NEWSUI228TODAYFEEDGROUPEMITTERFACTORY_H

@class SwiftObject;



@interface _TtC7NewsUI228TodayFeedGroupEmitterFactory : SwiftObject {
    ? newspaperGroupService;
    ? localNewsGroupService;
    ? formatService;
    ? audioHeadlineConverter;
    ? headlineService;
    ? topicClusterService;
    ? featureAvailability;
    ? magazineFeedService;
    ? personalizationService;
    ? subscriptionController;
    ? readingHistory;
    ? paidAccessChecker;
    ? tagService;
    ? tagHighlightsService;
    ? suggestionClusteringService;
    ? localNewsChannelService;
    ? engagementInlineViewManager;
    ? todayFeedEngagementEventHandler;
    ? engagementFeatureSettings;
    ? configurationManager;
    ? sportsTopicClusteringService;
    ? sportsDataService;
    ? sportsFavoritesService;
    ? sportsRecommendationService;
    ? sportsOnboardingManager;
    ? sportsGraphManager;
    ? sportsEventManager;
    ? sportsSectionService;
    ? sportsScoreSorter;
}






@end


#endif