// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTC9SEYMOURUI13SHOWCASESHELF_H
#define _TTC9SEYMOURUI13SHOWCASESHELF_H

@class SwiftObject;



@interface _TtC9SeymourUI13ShowcaseShelf : SwiftObject {
    ? delegate;
    ? identifier;
    ? row;
    ? recommendations;
    ? accountProvider;
    ? archivedSessionClient;
    ? bag;
    ? bookmarkClient;
    ? catalogClient;
    ? configurationClient;
    ? contentAvailabilityClient;
    ? dependencies;
    ? descriptor;
    ? eventHub;
    ? mediaTagStringBuilder;
    ? marketingItemProvider;
    ? recommendationClient;
    ? storefrontLocalizer;
    ? remoteBrowsingSource;
    ? engagementClient;
    ? subscriptionToken;
    ? subscriptionCache;
    ? multiUserEligibilityObserver;
    ? lazyLockupFetcher;
    ? lockupShelfItems;
    ? isHandlingAction;
    ? marketingOffer;
    ? config;
    ? serviceSubscription;
    ? matchingDeviceSubscriberAccount;
    ? deviceSubscriberAccount;
    ? deviceCloudAccount;
    ? accountHasWatch;
    ? resumableWorkoutSessions;
    ? marketingContents;
    ? completedMarketingVideoIdentifiers;
    ? bannerVideoPreviewDurationThresholdInSeconds;
    ? centerIdentifier;
}






@end


#endif