// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTC9SEYMOURUI7BURNBAR_H
#define _TTC9SEYMOURUI7BURNBAR_H

@class UIView;



@interface _TtC9SeymourUI7BurnBar : UIView {
    ? progress;
    ? expansion;
    ? scale;
    ? glow;
    ? rightInset;
    ? leftInset;
    ? summaryInset;
    ? pulseAnimation;
    ? displayMode;
    ? visibleBars;
    ? _circleCenterX;
    ? barGradient;
    ? pulseGradient;
    ? barLayer;
    ? outerBarLayer;
    ? glowDodgeLayer;
    ? modalityIconPlatter;
    ? pulseRate;
    ? state;
    ? pulseAnimator;
    ? animator;
    ? progressKey;
    ? expansionKey;
    ? scaleKey;
    ? glowKey;
    ? rightInsetKey;
    ? leftInsetKey;
}




-(id)initWithCoder:(id)arg0 ;
-(id)initWithFrame:(struct CGRect )arg0 ;
-(void)layoutSubviews;


@end


#endif