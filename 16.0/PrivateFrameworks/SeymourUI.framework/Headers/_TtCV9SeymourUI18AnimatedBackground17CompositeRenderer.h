// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTCV9SEYMOURUI18ANIMATEDBACKGROUND17COMPOSITERENDERER_H
#define _TTCV9SEYMOURUI18ANIMATEDBACKGROUND17COMPOSITERENDERER_H


#import <Foundation/Foundation.h>


@interface _TtCV9SeymourUI18AnimatedBackground17CompositeRenderer : NSObject {
    ? observer;
    ? spectrumAnalysis;
    ? colorPixelFormat;
    ? frameBufferPixelFormat;
    ? isBehindLyrics;
    ? state;
    ? environment;
    ? pinchMix;
    ? context;
    ? crossfadeDuration;
    ? crossfadeTimingFunction;
    ? modeTimingFunction;
    ? renderers;
    ? warpTimingSpeed;
    ? aspectRatio;
    ? currentContent;
    ? imageStorage;
    ? paused;
    ? placeholderColor;
    ? textureTransitionMix;
    ? uniforms;
}




-(id)init;


@end


#endif