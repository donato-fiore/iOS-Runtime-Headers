// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTC10CHRONOCORE17ACTIVITYVIEWMODEL_H
#define _TTC10CHRONOCORE17ACTIVITYVIEWMODEL_H

@class SwiftObject;



@interface _TtC10ChronoCore17ActivityViewModel : SwiftObject {
    ? activityDescriptor;
    ? defaultView;
    ? _viewEntryDidChangePublisher;
    ? viewEntryDidChangePublisher;
    ? viewEntry;
    ? viewCollection;
    ? canAppearInSecureEnvironment;
    ? isForeground;
    ? rawEnvironmentValues;
    ? metricsDefinition;
    ? prefersUnredactedContentInLowLuminanceEnvironment;
    ? supportsLowLuminance;
    ? _overrideDateDidChangePublisher;
    ? overrideDateDidChangePublisher;
    ? overrideDate;
    ? currentActionURL;
    ? handleLaunchRequest;
    ? logger;
    ? logIdentifier;
    ? _activityID;
    ? _activityService;
    ? _environmentProvider;
    ? _cancellable;
    ? _reloadSequenceNumber;
    ? _hasLoadedViewOnce;
    ? _viewSecurityPolicy;
    ? _viewSecurityPolicyDataSource;
}






@end


#endif