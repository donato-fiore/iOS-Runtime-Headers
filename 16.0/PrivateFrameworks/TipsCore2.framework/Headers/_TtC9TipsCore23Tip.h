// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTC9TIPSCORE23TIP_H
#define _TTC9TIPSCORE23TIP_H

@class SwiftObject;



@interface _TtC9TipsCore23Tip : SwiftObject {
    ? id;
    ? rules;
    ? options;
    ? testing;
    ? record;
    ? recordID;
    ? observers;
    ? actionHandler;
    ? transientValues;
    ? _display;
    ? _status;
    ? _shouldDisplay;
    ? _title;
    ? _message;
    ? _asset;
    ? _actions;
    ? _displayCount;
    ? _maxDisplayCount;
}






@end


#endif