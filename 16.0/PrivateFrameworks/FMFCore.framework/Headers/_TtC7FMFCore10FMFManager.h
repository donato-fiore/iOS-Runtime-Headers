// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.0.0 | SDK: 16.0.0


#ifndef _TTC7FMFCORE10FMFMANAGER_H
#define _TTC7FMFCORE10FMFMANAGER_H

@class SwiftObject;



@interface _TtC7FMFCore10FMFManager : SwiftObject {
    ? identifier;
    ? delegate;
    ? fmfManagerSerialQueue;
    ? configuration;
    ? dataManager;
    ? secureLocationsManager;
    ? garbageCollector;
    ? systemStateController;
    ? genericUpdatesHandler;
    ? refreshController;
    ? interactionController;
    ? actionsController;
    ? myLocationController;
    ? proactiveLocationController;
    ? secureLocationRefreshController;
    ? reverseGeocoder;
    ? isRefreshing;
    ? isSnapshotMode;
    ? snapshotResponseReceived;
    ? selectedFriend;
    ? isInitialized;
}






@end


#endif