// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 15.0.0 | SDK: 15.0.0


#ifndef _TTC15MANAGEDSETTINGS20MANAGEDSETTINGSSTORE_H
#define _TTC15MANAGEDSETTINGS20MANAGEDSETTINGSSTORE_H

@class SwiftObject;



@interface _TtC15ManagedSettings20ManagedSettingsStore : SwiftObject {
    ? effectiveMediaSettingsNotifier;
    ? account;
    ? allowedClient;
    ? application;
    ? appStore;
    ? cellular;
    ? dateAndTime;
    ? gameCenter;
    ? media;
    ? passcode;
    ? safari;
    ? shield;
    ? siri;
    ? webContent;
    ? _effectiveMaximumMovieRating;
    ? _effectiveMaximumTVShowRating;
}






@end


#endif