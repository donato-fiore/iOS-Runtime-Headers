// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 15.0.0 | SDK: 15.0.0


#ifndef _TTC12APPANALYTICS7TRACKER_H
#define _TTC12APPANALYTICS7TRACKER_H

@class SwiftObject;



@interface _TtC12AppAnalytics7Tracker : SwiftObject {
    ? name;
    ? identifier;
    ? delegate;
    ? accessQueue;
    ? accessGroup;
    ? eventMirrorStore;
    ? timers;
    ? restartTimers;
    ? dataStacks;
    ? parentTracker;
    ? groupStack;
    ? currentActionEntries;
    ? previousActionSequence;
    ? willSubmitObservers;
    ? gracefulEventCreation;
    ? privacyValidationProvider;
}






@end


#endif