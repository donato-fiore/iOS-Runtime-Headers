// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 15.0.0 | SDK: 15.0.0


#ifndef _TTC17MEASUREFOUNDATION14SURFACETEXTURE_H
#define _TTC17MEASUREFOUNDATION14SURFACETEXTURE_H


#import <Foundation/Foundation.h>


@interface _TtC17MeasureFoundation14SurfaceTexture : NSObject {
    ? pixelFormat;
    ? bitsPerComponent;
    ? width;
    ? height;
    ? texture;
    ? isLocking;
    ? backgroundColor;
    ? color;
    ? cursorX;
    ? cursorY;
    ? bytesPerRow;
    ? pixelsPerRow;
    ? pixel32s;
    ? pixelBytes;
    ? bytesPerPixel;
    ? pixelsNum;
    ? mtlDevice;
    ? ioSurface;
}




-(id)init;


@end


#endif