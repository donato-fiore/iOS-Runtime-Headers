// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 15.0.0 | SDK: 15.0.0


#ifndef _TTC7VFXCORE20RENDERCOMMANDENCODER_H
#define _TTC7VFXCORE20RENDERCOMMANDENCODER_H

@class SwiftObject;



@interface _TtC7VFXCore20RenderCommandEncoder : SwiftObject {
    ? encoder;
    ? renderPassFormat;
    ? bufferPool;
    ? workerIndex;
    ? counterRecorder;
    ? winding;
    ? cullMode;
    ? fillMode;
    ? clipMode;
    ? stencilReferenceValueFront;
    ? stencilReferenceValueBack;
    ? inverseWinding;
    ? windingChanged;
    ? cullModeChanged;
    ? fillModeChanged;
    ? clipModeChanged;
    ? lockCullMode;
    ? lockFillMode;
    ? vertexBuffers;
    ? vertexTextures;
    ? vertexSamplers;
    ? fragmentBuffers;
    ? fragmentTextures;
    ? fragmentSamplers;
    ? depthStencilStates;
    ? renderPipelineState;
}






@end


#endif