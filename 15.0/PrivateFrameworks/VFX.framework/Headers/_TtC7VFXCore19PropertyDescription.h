// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 15.0.0 | SDK: 15.0.0


#ifndef _TTC7VFXCORE19PROPERTYDESCRIPTION_H
#define _TTC7VFXCORE19PROPERTYDESCRIPTION_H

@class SwiftObject;



@interface _TtC7VFXCore19PropertyDescription : SwiftObject {
    ? name;
    ? accessorCreator;
    ? componentType;
    ? getset;
    ? keyPath;
    ? visibility;
    ? unavailableStartingFromVersion;
    ? lookupReplacementProperty;
    ? cachedResolvedReplacementProperty;
    ? semantic;
    ? type;
    ? defaultValues;
    ? userManual;
    ? _min;
    ? _max;
    ? _step;
    ? _minFractionDigit;
    ? _maxFractionDigit;
    ? _offset;
    ? _size;
    ? _stride;
    ? _alignment;
    ? flags;
    ? _scriptingProperty;
}






@end


#endif