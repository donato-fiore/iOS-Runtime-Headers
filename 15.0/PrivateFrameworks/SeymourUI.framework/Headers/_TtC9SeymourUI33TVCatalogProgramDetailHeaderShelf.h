// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 15.0.0 | SDK: 15.0.0


#ifndef _TTC9SEYMOURUI33TVCATALOGPROGRAMDETAILHEADERSHELF_H
#define _TTC9SEYMOURUI33TVCATALOGPROGRAMDETAILHEADERSHELF_H

@class SwiftObject;



@interface _TtC9SeymourUI33TVCatalogProgramDetailHeaderShelf : SwiftObject {
    ? delegate;
    ? identifier;
    ? row;
    ? programDetail;
    ? bag;
    ? bookmarkClient;
    ? catalogClient;
    ? dependencies;
    ? mediaTagStringBuilder;
    ? recommendationClient;
    ? remoteBrowsingSource;
    ? storefrontLocalizer;
    ? multiUserEligibilityObserver;
    ? placeholders;
    ? lazyLockupFetcher;
    ? isHandlingAction;
    ? multiUserEligibilitySubscription;
    ? marketingOffer;
    ? config;
    ? serviceSubscription;
    ? matchingDeviceSubscriberAccount;
    ? deviceSubscriberAccount;
    ? deviceCloudAccount;
    ? accountHasWatch;
}






@end


#endif