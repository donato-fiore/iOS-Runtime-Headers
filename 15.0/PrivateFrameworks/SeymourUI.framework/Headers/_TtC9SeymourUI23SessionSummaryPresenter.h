// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 15.0.0 | SDK: 15.0.0


#ifndef _TTC9SEYMOURUI23SESSIONSUMMARYPRESENTER_H
#define _TTC9SEYMOURUI23SESSIONSUMMARYPRESENTER_H

@class SwiftObject;



@interface _TtC9SeymourUI23SessionSummaryPresenter : SwiftObject {
    ? display;
    ? onTapExit;
    ? artworkImageLoader;
    ? bookmarkClient;
    ? catalogClient;
    ? sessionClient;
    ? eventHub;
    ? subscriptionToken;
    ? platform;
    ? activityTypeBehavior;
    ? healthKitActivityType;
    ? catalogWorkout;
    ? dependencies;
    ? stringBuilder;
    ? unitPreferencesProvider;
    ? displayPreferenceClient;
    ? privacyPreferenceClient;
    ? remoteBrowsingSource;
    ? standaloneWorkoutTimeProvider;
    ? activityRings;
    ? userMassMetric;
    ? displayPreferences;
    ? gymKitConnection;
    ? playback;
    ? watchConnection;
    ? workout;
    ? presentationTimestamp;
    ? distanceMetric;
    ? mindfulMinutesMetric;
    ? paceMetric;
    ? energyMetric;
    ? heartRateMetric;
    ? mostRecentScoreMetric;
    ? state;
    ? textSizeChangedObserver;
}




-(void)textSizeChanged:(id)arg0 ;


@end


#endif