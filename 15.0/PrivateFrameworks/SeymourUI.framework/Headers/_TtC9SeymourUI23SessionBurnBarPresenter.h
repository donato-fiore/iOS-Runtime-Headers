// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 15.0.0 | SDK: 15.0.0


#ifndef _TTC9SEYMOURUI23SESSIONBURNBARPRESENTER_H
#define _TTC9SEYMOURUI23SESSIONBURNBARPRESENTER_H

@class SwiftObject;



@interface _TtC9SeymourUI23SessionBurnBarPresenter : SwiftObject {
    ? display;
    ? _activityRings;
    ? activityRingsSubscriptionToken;
    ? activityTypeBehavior;
    ? lastReceivedScoreMetric;
    ? state;
    ? timer;
    ? lastPackPosition;
    ? watchConnection;
    ? _cumulativeCommunityScores;
    ? _rollingCommunityScores;
    ? _userBodyMassMetric;
    ? cumulativeScoreGenerator;
    ? cumulativeScoreGeneratorSubscription;
    ? rollingScoreGenerator;
    ? rollingScoreGeneratorSubscription;
    ? rollingScoreMetricSubscription;
    ? _alwaysFakeCommunityScores;
    ? _fakeCommunityScoresOnFailure;
    ? catalogWorkout;
    ? scoreClient;
    ? dependencies;
    ? eventHub;
    ? networkEvaluator;
    ? sessionToken;
    ? subscriptionToken;
    ? pulseMonitor;
    ? pulseSubscription;
}






@end


#endif