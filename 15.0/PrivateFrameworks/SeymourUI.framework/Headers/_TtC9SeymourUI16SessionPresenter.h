// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 15.0.0 | SDK: 15.0.0


#ifndef _TTC9SEYMOURUI16SESSIONPRESENTER_H
#define _TTC9SEYMOURUI16SESSIONPRESENTER_H

@class SwiftObject;



@interface _TtC9SeymourUI16SessionPresenter : SwiftObject {
    ? display;
    ? backgroundPresenter;
    ? bufferPresenter;
    ? castingPresenter;
    ? displayPreferencesPresenter;
    ? instructionsPresenter;
    ? introSequencePresenter;
    ? overlayPresenter;
    ? mediaPlayerPresenter;
    ? routePickerPresenter;
    ? summaryPresenter;
    ? watchDiscoveryPresenter;
    ? continueOnWatchPresenter;
    ? ongoingWorkoutDetectedPresenter;
    ? activityTypeBehavior;
    ? assetClient;
    ? catalogWorkout;
    ? eventHub;
    ? multiUserClient;
    ? catalogClient;
    ? mediaTagStringBuilder;
    ? orientationObserver;
    ? platform;
    ? presentationCoordinator;
    ? remoteBrowsingSource;
    ? sessionClient;
    ? sessionOrigin;
    ? sessionPowerLog;
    ? startTime;
    ? subscriptionToken;
    ? timeline;
    ? timerProvider;
    ? unitPreferencesProvider;
    ? standaloneWorkoutTimeProvider;
    ? playback;
    ? startDate;
    ? watchConnectionState;
    ? countdown;
    ? initiatedGroupActivity;
    ? gymKitConnection;
    ? termination;
    ? workout;
    ? state;
}






@end


#endif