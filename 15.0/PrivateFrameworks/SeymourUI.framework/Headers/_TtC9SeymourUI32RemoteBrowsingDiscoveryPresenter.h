// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 15.0.0 | SDK: 15.0.0


#ifndef _TTC9SEYMOURUI32REMOTEBROWSINGDISCOVERYPRESENTER_H
#define _TTC9SEYMOURUI32REMOTEBROWSINGDISCOVERYPRESENTER_H

@class SwiftObject;



@interface _TtC9SeymourUI32RemoteBrowsingDiscoveryPresenter : SwiftObject {
    ? display;
    ? eventHub;
    ? subscriptionToken;
    ? timerProvider;
    ? participantClient;
    ? platform;
    ? remoteBrowsingClient;
    ? accountProvider;
    ? avatarFetcher;
    ? state;
    ? role;
    ? timeoutTimer;
    ? currentSnapshot;
    ? isShowingSkipButton;
    ? discoveredParticipants;
    ? fetchedAvatars;
    ? onDiscoveryTimeout;
    ? onParticipantConnected;
    ? onNewGuestFlowSelected;
    ? onSkipSelected;
}






@end


#endif