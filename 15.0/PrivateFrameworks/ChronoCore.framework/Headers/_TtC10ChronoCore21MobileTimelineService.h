// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 15.0.0 | SDK: 15.0.0


#ifndef _TTC10CHRONOCORE21MOBILETIMELINESERVICE_H
#define _TTC10CHRONOCORE21MOBILETIMELINESERVICE_H

@class SwiftObject;



@interface _TtC10ChronoCore21MobileTimelineService : SwiftObject {
    ? cacheManager;
    ? configurationService;
    ? descriptorProvider;
    ? locationService;
    ? extensionManager;
    ? environmentProvidingFactory;
    ? statefulStore;
    ? statefulStoreUI;
    ? lock;
    ? logger;
    ? persistentSubscriptions;
    ? environmentSubscriptions;
    ? configurationLifetimeAssertions;
    ? widgetKeysNeedingPrivacyContent;
    ? processMonitor;
}






@end


#endif