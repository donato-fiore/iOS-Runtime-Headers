// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 15.0.0 | SDK: 15.0.0


#ifndef _TTCV10PEGASUSAPI15KGQSAPIPB_THINGP33_967B2FE05D2E8B937F63C8F80722C13913_STORAGECLASS_H
#define _TTCV10PEGASUSAPI15KGQSAPIPB_THINGP33_967B2FE05D2E8B937F63C8F80722C13913_STORAGECLASS_H

@class SwiftObject;



@interface _TtCV10PegasusAPI15Kgqsapipb_ThingP33_967B2FE05D2E8B937F63C8F80722C13913_StorageClass : SwiftObject {
    ? _id;
    ? _title;
    ? _redirects;
    ? _disambiguations;
    ? _titleCleaned;
    ? _redirectsCleaned;
    ? _disambiguationsCleaned;
    ? _anchorText;
    ? _alternativeReadings;
    ? _shortAbstract;
    ? _birthDate;
    ? _deathDate;
    ? _birthPlaces;
    ? _deathPlaces;
    ? _spouses;
    ? _children;
    ? _parents;
    ? _height;
    ? _homepageurl;
    ? _wikipageurl;
    ? _thumbnailurl;
    ? _foundingDate;
    ? _governingBody;
    ? _length;
    ? _width;
    ? _nationalAnthem;
    ? _mottos;
    ? _population;
    ? _currency;
    ? _capitalcity;
    ? _latitude;
    ? _longitude;
    ? _nearestCity;
    ? _numberOfVisitors;
    ? _visitYear;
    ? _largestCity;
    ? _languages;
    ? _pageviews;
    ? _index;
    ? _isDisputedTerritory;
    ? _englishLanguageLink;
    ? _dbpediaClasses;
    ? _wikipediaCategories;
    ? _titleQualifier;
    ? _locale;
    ? _modtimeSecs;
    ? _indexedTimestamp;
    ? _runtime;
    ? _releaseDate;
    ? _ingredient;
    ? _numberOfEmployees;
    ? _numberOfStudents;
    ? _date;
    ? _revID;
    ? _thumbNail3XURL;
    ? _thumbNailNanoURLN27;
    ? _thumbNailNanoURLN28;
}






@end


#endif