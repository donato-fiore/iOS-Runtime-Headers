// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 15.0.0 | SDK: 15.0.0


#ifndef _TTC9COREIDVUI27IDENTITYPROOFINGFLOWMANAGER_H
#define _TTC9COREIDVUI27IDENTITYPROOFINGFLOWMANAGER_H

@class SwiftObject;



@interface _TtC9CoreIDVUI27IdentityProofingFlowManager : SwiftObject {
    ? issuerSubRegion;
    ? idSubRegion;
    ? idTypeProper;
    ? idTypeLower;
    ? issuerName;
    ? delegate;
    ? connection;
    ? documentsRepository;
    ? proofingUIThreshold;
    ? proofingOptions;
    ? viewProvider;
    ? eligibilityHelper;
    ? configuration;
    ? termsAndConditionsViewController;
    ? unorderedUI;
    ? idType;
    ? attributeMessages;
    ? messageReplacements;
    ? state;
    ? country;
    ? livenessSessionManager;
    ? textReplacementHelper;
    ? attributeConfigHelper;
    ? termsAndConditionsAccessQueue;
    ? needsHSA2Upgrade;
    ? accountVerificationError;
    ? accountVerificationGroup;
    ? livenenessConfigFetchGroup;
    ? livenessConfigFetchError;
    ? livenessConfig;
    ? learnMoreURL;
    ? _passcode;
    ? analyticsReporter;
    ? cardArtData;
    ? _termsAndConditions;
}






@end


#endif