// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC8VIDEOSUI5ROUTE_H
#define _TTC8VIDEOSUI5ROUTE_H

@class SwiftObject;



@interface _TtC8VideosUI5Route : SwiftObject {
    ? id;
    ? viewModel;
    ? transaction;
    ? localizationManager;
    ? isNavigationActiveDidChange;
    ? isPresentingDidChange;
    ? viewMetricsRecorder;
    ? isRoutable;
    ? _isPresentingPopover;
    ? _isNavigationActive;
    ? _isPresentingSheet;
    ? _isPresentingFullscreenCover;
    ? _isPresentingAlert;
    ? alertPresentationContext;
    ? eventDataSource;
    ? documentDataSource;
    ? nextEventType;
}






@end


#endif