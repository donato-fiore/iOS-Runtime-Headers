// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC17MEASUREFOUNDATION14DEPTHPROCESSOR_H
#define _TTC17MEASUREFOUNDATION14DEPTHPROCESSOR_H

@class SwiftObject;



@interface _TtC17MeasureFoundation14DepthProcessor : SwiftObject {
    ? minConfidence;
    ? longRange;
    ? shortRange;
    ? quadTreeCellSize;
    ? currentRange;
    ? viewport;
    ? orientation;
    ? session;
    ? quadTree;
    ? allJasperPoints;
    ? numPointsInMap;
    ? lastPointCloud;
    ? cameraToWorld;
    ? $__lazy_storage_$_transformer;
    ? adProcessor;
    ? lock;
    ? numKeyframes;
    ? minCameraTranslation;
    ? debugOptions;
}






@end


#endif