// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC22IMAP2CONNECTIONCONTROL6ENGINE_H
#define _TTC22IMAP2CONNECTIONCONTROL6ENGINE_H

@class SwiftObject;



@interface _TtC22IMAP2ConnectionControl6Engine : SwiftObject {
    ? workloop;
    ? persistenceEventQueue;
    ? sendAction;
    ? logger;
    ? logName;
    ? tracingIDOwner;
    ? state;
    ? connections;
    ? betterPathEvaluator;
    ? didStart;
    ? isProcessingUpdates;
    ? pendingUpdates;
    ? externalState;
    ? activityTimer;
    ? didTearDown;
    ? stateCapture;
}






@end


#endif