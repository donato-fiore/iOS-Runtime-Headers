// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC15IMAP2CONNECTION17COMMANDCONNECTION_H
#define _TTC15IMAP2CONNECTION17COMMANDCONNECTION_H

@class SwiftObject;



@interface _TtC15IMAP2Connection17CommandConnection : SwiftObject {
    ? connection;
    ? queue;
    ? logger;
    ? delegate;
    ? commandCounter;
    ? runningCommands;
    ? id;
    ? engineID;
    ? state;
    ? enqueuedEvents;
    ? idleKind;
    ? idleTimer;
    ? idleRefreshInterval;
    ? makeCredentials;
    ? getPushInformation;
    ? finishPushRegistration;
    ? pathAttributes;
}






@end


#endif