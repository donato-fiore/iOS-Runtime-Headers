// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC8NEWSFEED20FORMATIMAGENODESTYLE_H
#define _TTC8NEWSFEED20FORMATIMAGENODESTYLE_H

@class SwiftObject;



@interface _TtC8NewsFeed20FormatImageNodeStyle : SwiftObject {
    ? _identifier;
    ? class;
    ? _selectors;
    ? alpha;
    ? _adjustments;
    ? background;
    ? borders;
    ? contentMode;
    ? cornerRadius;
    ? renderingMode;
    ? resize;
    ? shadow;
    ? tintColor;
    ? ignoresSmartInvertColors;
    ? request;
    ? imageMask;
}






@end


#endif