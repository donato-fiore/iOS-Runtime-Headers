// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC8NEWSFEED14FORMATTEXTNODE_H
#define _TTC8NEWSFEED14FORMATTEXTNODE_H

@class SwiftObject;



@interface _TtC8NewsFeed14FormatTextNode : SwiftObject {
    ? _identifier;
    ? size;
    ? resize;
    ? _adjustments;
    ? style;
    ? animation;
    ? content;
    ? invalidation;
    ? _zIndex;
    ? _visibility;
    ? _isUserInteractionEnabled;
    ? accessibility;
    ? accessibilityRole;
    ? _selectors;
    ? name;
}






@end


#endif