// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC8NEWSFEED21FORMATVIDEOPLAYERNODE_H
#define _TTC8NEWSFEED21FORMATVIDEOPLAYERNODE_H

@class SwiftObject;



@interface _TtC8NewsFeed21FormatVideoPlayerNode : SwiftObject {
    ? _identifier;
    ? size;
    ? resize;
    ? _adjustments;
    ? style;
    ? animation;
    ? content;
    ? posterContent;
    ? posterStyle;
    ? overlayContent;
    ? isLive;
    ? _zIndex;
    ? _visibility;
    ? _isUserInteractionEnabled;
    ? accessibilityRole;
    ? _selectors;
    ? _name;
    ? allowsPrerollAds;
}






@end


#endif