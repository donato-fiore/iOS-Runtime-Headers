// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC7VFXCORE8DRAWCALL_H
#define _TTC7VFXCORE8DRAWCALL_H

@class SwiftObject;



@interface _TtC7VFXCore8DrawCall : SwiftObject {
    ? primitiveType;
    ? meshIdentifier;
    ? indexBuffer;
    ? indexType;
    ? indexCount;
    ? vertexCount;
    ? vertexOffset;
    ? instanceCount;
    ? instanceOffset;
    ? viewportCount;
    ? stencilReferenceValueFront;
    ? stencilReferenceValueBack;
    ? winding;
    ? cullMode;
    ? fillMode;
    ? clipMode;
    ? needPBR;
    ? depthStencilStates;
    ? renderPipelineState;
    ? arguments;
}






@end


#endif