// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTCO7VFXCORE14CODEGENERATION17CODESOURCESNIPPET_H
#define _TTCO7VFXCORE14CODEGENERATION17CODESOURCESNIPPET_H

@class SwiftObject;



@interface _TtCO7VFXCore14CodeGeneration17CodeSourceSnippet : SwiftObject {
    ? name;
    ? label;
    ? searchLabel;
    ? tooltip;
    ? hasValue;
    ? includeFunctions;
    ? supportedBy;
    ? useNamedSignature;
    ? hidden;
    ? skipTest;
    ? disableable;
    ? timeDependent;
    ? useDynamicBranching;
    ? deprecated;
    ? deprecationInfo;
    ? toolCreateFunction;
    ? genericTypes;
    ? previousInParameterNames;
    ? inParameterNames;
    ? inParameterTypes;
    ? inParameterDefaultValues;
    ? showInParameterDefaultValues;
    ? mandatoryInParameters;
    ? outParameterNames;
    ? outParameterTypes;
    ? code;
    ? cleanupCode;
    ? noOutputDeclarationCode;
    ? codeContainsUidIndexTag;
    ? valueType;
    ? defaultInParameter;
    ? inParameters;
    ? outParameters;
    ? libraryName;
    ? libraryColor;
    ? includeFunctionsDependencies;
}






@end


#endif