// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC7VFXCORE11SHADERCACHE_H
#define _TTC7VFXCORE11SHADERCACHE_H

@class SwiftObject;



@interface _TtC7VFXCore11ShaderCache : SwiftObject {
    ? device;
    ? defaultLibrary;
    ? renderPipelines;
    ? computePipelines;
    ? inactiveByPipeline;
    ? disableCache;
    ? statistics;
    ? lastStatistics;
    ? renderPipelineStats;
    ? computePipelineStats;
    ? recordingBinaryArchive;
    ? precompiledBinaryArchives;
    ? urlToSaveBinaryArchiveTo;
    ? asyncCompiler;
    ? lock;
    ? reloadCallback;
}






@end


#endif