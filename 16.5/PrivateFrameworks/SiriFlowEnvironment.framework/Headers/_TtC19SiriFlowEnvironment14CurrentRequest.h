// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC19SIRIFLOWENVIRONMENT14CURRENTREQUEST_H
#define _TTC19SIRIFLOWENVIRONMENT14CURRENTREQUEST_H

@class SwiftObject;



@interface _TtC19SiriFlowEnvironment14CurrentRequest : SwiftObject {
    ? mock;
    ? _refId;
    ? _aceId;
    ? _executionRequestId;
    ? _sessionHandoffContinuityID;
    ? _peerName;
    ? _inputOrigin;
    ? _interactionType;
    ? _interactionId;
    ? _audioSource;
    ? _audioDestination;
    ? _responseMode;
    ? _multiUserState;
    ? _bargeInContext;
    ? _sharedContextService;
    ? _originatingHome;
    ? _speechPackage;
    ? _resultCandidateId;
    ? _asrOnDevice;
    ? _currentDeviceAssistantId;
    ? _voiceTriggerEventInfo;
    ? _companionName;
    ? _isRecognizeMyVoiceEnabled;
    ? _flowTaskMetadata;
    ? _voiceAudioSessionId;
    ? _utterance;
}






@end


#endif