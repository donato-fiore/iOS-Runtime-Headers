// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC10STOCKSCORE16WATCHLISTMANAGER_H
#define _TTC10STOCKSCORE16WATCHLISTMANAGER_H

@class SwiftObject;



@interface _TtC10StocksCore16WatchlistManager : SwiftObject {
    ? activeWatchlistSubject;
    ? lazyPrewarm;
    ? userDefaults;
    ? accessLock;
    ? fetchQueue;
    ? watchlistOrder;
    ? watchlistMap;
    ? watchlistService;
    ? metadataManager;
    ? observers;
    ? featureAvailability;
    ? timer;
    ? cancellables;
}






@end


#endif