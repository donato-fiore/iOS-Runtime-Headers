// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTCC16MUSICKITINTERNAL19INTERNALMUSICPLAYER5QUEUE_H
#define _TTCC16MUSICKITINTERNAL19INTERNALMUSICPLAYER5QUEUE_H

@class SwiftObject;



@interface _TtCC16MusicKitInternal19InternalMusicPlayer5Queue : SwiftObject {
    ? pendingEntries;
    ? pendingStartEntry;
    ? components;
    ? kind;
    ? player;
    ? playActivityFeatureName;
    ? playActivityRecommendationData;
    ? playableItems;
    ? playableRequest;
    ? stationSeed;
    ? startItem;
    ? playbackSource;
    ? transientShuffleMode;
    ? transientRepeatMode;
    ? transientAutoPlayEnabled;
    ? properties;
    ? activeMonitoringPublisher;
}






@end


#endif