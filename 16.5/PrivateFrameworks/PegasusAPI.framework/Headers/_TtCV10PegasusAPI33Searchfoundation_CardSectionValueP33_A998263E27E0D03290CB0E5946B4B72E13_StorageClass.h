// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTCV10PEGASUSAPI33SEARCHFOUNDATION_CARDSECTIONVALUEP33_A998263E27E0D03290CB0E5946B4B72E13_STORAGECLASS_H
#define _TTCV10PEGASUSAPI33SEARCHFOUNDATION_CARDSECTIONVALUEP33_A998263E27E0D03290CB0E5946B4B72E13_STORAGECLASS_H

@class SwiftObject;



@interface _TtCV10PegasusAPI33Searchfoundation_CardSectionValueP33_A998263E27E0D03290CB0E5946B4B72E13_StorageClass : SwiftObject {
    ? _appLinkCardSection;
    ? _descriptionCardSection;
    ? _keyValueDataCardSection;
    ? _mapCardSection;
    ? _mediaInfoCardSection;
    ? _mediaPlayerCardSection;
    ? _nowPlayingCardSection;
    ? _richTitleCardSection;
    ? _rowCardSection;
    ? _scoreboardCardSection;
    ? _socialMediaPostCardSection;
    ? _stockChartCardSection;
    ? _tableHeaderRowCardSection;
    ? _tableRowCardSection;
    ? _textColumnsCardSection;
    ? _titleCardSection;
    ? _trackListCardSection;
    ? _audioPlaybackCardSection;
    ? _flightCardSection;
    ? _activityIndicatorCardSection;
    ? _webCardSection;
    ? _messageCardSection;
    ? _detailedRowCardSection;
    ? _imagesCardSection;
    ? _suggestionCardSection;
    ? _selectableGridCardSection;
    ? _sectionHeaderCardSection;
    ? _metaInfoCardSection;
    ? _watchListCardSection;
    ? _mapsDetailedRowCardSection;
    ? _buttonCardSection;
    ? _horizontalButtonCardSection;
    ? _verticalLayoutCardSection;
    ? _productCardSection;
    ? _horizontalScrollCardSection;
    ? _mediaRemoteControlCardSection;
    ? _mapPlaceCardSection;
    ? _compactRowCardSection;
    ? _worldMapCardSection;
    ? _attributionFooterCardSection;
    ? _gridCardSection;
    ? _personHeaderCardSection;
    ? _colorBarCardSection;
    ? _splitCardSection;
    ? _linkPresentationCardSection;
    ? _findMyCardSection;
    ? _heroCardSection;
    ? _newsCardSection;
    ? _miniCardSection;
    ? _infoCardSection;
    ? _collectionCardSection;
    ? _combinedCardSection;
    ? _responseWrapperCardSection;
    ? _listenToCardSection;
    ? _watchNowCardSection;
    ? _strokeAnimationCardSection;
    ? _buttonListCardSection;
    ? _commandRowCardSection;
    ? _leadingTrailingCardSection;
    ? _heroTitleCardSection;
    ? _archiveViewCardSection;
    ? _appIconCardSection;
}






@end


#endif