// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC11PRIDEPOSTER22PRIDEPARTICLESRENDERER_H
#define _TTC11PRIDEPOSTER22PRIDEPARTICLESRENDERER_H

@class SwiftObject;



@interface _TtC11PridePoster22PrideParticlesRenderer : SwiftObject {
    ? isSnapshotting;
    ? significantChangeHandler;
    ? configurationIdentifier;
    ? appearance;
    ? layer;
    ? device;
    ? commandQueue;
    ? colorFormat;
    ? depthFormat;
    ? multisampleCount;
    ? depthState;
    ? colorTexture;
    ? depthTexture;
    ? snapshotTexture;
    ? overlayLayer;
    ? particulator;
    ? driver;
    ? lastSettledOffset;
    ? lastUnlockTransitionWasToUnlocked;
    ? lastNonSnapshotAppearance;
    ? defaultsKey;
}






@end


#endif