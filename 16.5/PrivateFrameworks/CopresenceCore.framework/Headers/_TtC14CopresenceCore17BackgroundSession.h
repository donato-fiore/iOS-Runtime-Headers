// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC14COPRESENCECORE17BACKGROUNDSESSION_H
#define _TTC14COPRESENCECORE17BACKGROUNDSESSION_H

@class SwiftObject;



@interface _TtC14CopresenceCore17BackgroundSession : SwiftObject {
    ? $defaultActor;
    ? id;
    ? activity;
    ? reporter;
    ? delegate;
    ? $__lazy_storage_$_protectedStorage;
    ? state;
    ? session;
    ? service;
    ? queue;
    ? members;
    ? suspensionAssertions;
    ? cancellables;
    ? localMember;
    ? sessionProvider;
    ? serverBag;
    ? presenceController;
    ? sessionSuspensionController;
}






@end


#endif