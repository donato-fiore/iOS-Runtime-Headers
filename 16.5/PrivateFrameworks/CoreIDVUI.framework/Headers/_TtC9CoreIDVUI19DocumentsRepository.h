// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC9COREIDVUI19DOCUMENTSREPOSITORY_H
#define _TTC9COREIDVUI19DOCUMENTSREPOSITORY_H

@class SwiftObject;



@interface _TtC9CoreIDVUI19DocumentsRepository : SwiftObject {
    ? documentFront;
    ? documentBack;
    ? selfie;
    ? idFrontMetrics;
    ? idBackMetrics;
    ? selfieMetrics;
    ? livenessMetrics;
    ? idFrontSettings;
    ? idBackSettings;
    ? selfieSettings;
    ? livenessSettings;
    ? resizeDimensions;
    ? exploration;
    ? frameBufferSize;
    ? videoFrameRate;
    ? pdf417Data;
    ? liveness;
    ? queue;
    ? idFrontFallbackSettings;
    ? idBackFallbackSettings;
    ? selfieFallbackSettings;
    ? livenessFallbackSettings;
    ? idFrontFallbackDimensions;
    ? idBackFallbackDimensions;
    ? selfieFallbackDimensions;
    ? fallbackBufferSize;
    ? fallbackFrameRate;
    ? maxNumMetrics;
    ? luxMonitor;
}






@end


#endif