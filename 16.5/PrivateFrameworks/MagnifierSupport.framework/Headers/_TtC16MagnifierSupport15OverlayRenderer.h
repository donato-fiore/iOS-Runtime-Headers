// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC16MAGNIFIERSUPPORT15OVERLAYRENDERER_H
#define _TTC16MAGNIFIERSUPPORT15OVERLAYRENDERER_H

@class SwiftObject;



@interface _TtC16MagnifierSupport15OverlayRenderer : SwiftObject {
    ? debugOption;
    ? isCenterDetectionEnabled;
    ? centerDetectionSize;
    ? session;
    ? _device;
    ? _inFlightSemaphore;
    ? _mtkView;
    ? _commandQueue;
    ? _personDepthVertexBuffer;
    ? _personDepthPipelineState;
    ? _personDepthTexture;
    ? _backWideTexture;
    ? _labelTexture;
    ? _maxLabel;
    ? _ciContext;
    ? _colorSpace;
    ? _detectedPersonRectArray;
    ? _debugBufferWidth;
    ? _debugBufferHeight;
    ? _personDepthTextureCache;
    ? _labelTextureCache;
    ? _viewportSize;
    ? _viewportSizeDidChange;
}






@end


#endif