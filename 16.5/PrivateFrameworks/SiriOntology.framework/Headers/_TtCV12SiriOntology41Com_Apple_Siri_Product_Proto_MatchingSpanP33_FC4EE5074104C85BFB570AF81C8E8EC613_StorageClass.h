// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTCV12SIRIONTOLOGY41COM_APPLE_SIRI_PRODUCT_PROTO_MATCHINGSPANP33_FC4EE5074104C85BFB570AF81C8E8EC613_STORAGECLASS_H
#define _TTCV12SIRIONTOLOGY41COM_APPLE_SIRI_PRODUCT_PROTO_MATCHINGSPANP33_FC4EE5074104C85BFB570AF81C8E8EC613_STORAGECLASS_H

@class SwiftObject;



@interface _TtCV12SiriOntology41Com_Apple_Siri_Product_Proto_MatchingSpanP33_FC4EE5074104C85BFB570AF81C8E8EC613_StorageClass : SwiftObject {
    ? _label;
    ? _serializedValue;
    ? _input;
    ? _matchedTerm;
    ? _prefix;
    ? _suffix;
    ? _start;
    ? _end;
    ? _startWithoutPrefix;
    ? _endWithoutSuffix;
    ? _confidence;
    ? _prefixSuffixConfidence;
    ? _explicit;
    ? _isSuffixVariableMatch;
    ? _isPrefixVariableMatch;
    ? _isPayloadVariableMatch;
    ? _locale;
    ? _numWords;
    ? _matchType;
    ? _appID;
    ? _matcherNames;
    ? _tags;
    ? _altInterpretationConfidences;
    ? _altInterpretationPhoneSequences;
    ? _metadata;
    ? _preItnphrasesStartTime;
    ? _preItnphrasesEndTime;
}






@end


#endif