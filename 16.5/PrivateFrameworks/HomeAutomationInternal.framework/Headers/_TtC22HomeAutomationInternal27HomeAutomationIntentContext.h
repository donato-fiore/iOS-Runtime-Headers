// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC22HOMEAUTOMATIONINTERNAL27HOMEAUTOMATIONINTENTCONTEXT_H
#define _TTC22HOMEAUTOMATIONINTERNAL27HOMEAUTOMATIONINTENTCONTEXT_H

@class SwiftObject;



@interface _TtC22HomeAutomationInternal27HomeAutomationIntentContext : SwiftObject {
    ? mockGlobals;
    ? hasAllQuantifier;
    ? smallestUserMentionedContainerLayer;
    ? isRemoteHomeRequest;
    ? attribute;
    ? entityNames;
    ? roomNames;
    ? zoneNames;
    ? targetedHomeName;
    ? deviceTypes;
    ? targetState;
    ? isSensorTrippedRequest;
    ? isGarageDoorRequest;
    ? userDefinedDuration;
    ? isOpenRequest;
    ? isCloseRequest;
    ? isLockRequest;
    ? isUnlockRequest;
    ? isDisarmRequest;
    ? isTVRequest;
    ? numericValue;
    ? isInCurrentRoom;
    ? isColorRequest;
    ? isScheduleRequest;
    ? isSceneRequest;
    ? targetColor;
}






@end


#endif