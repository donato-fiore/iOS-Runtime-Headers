// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTCV10XAVIERNEWS7CLASSIC20HEADLINESCOREPROFILE_H
#define _TTCV10XAVIERNEWS7CLASSIC20HEADLINESCOREPROFILE_H

@class SwiftObject;



@interface _TtCV10XavierNews7Classic20HeadlineScoreProfile : SwiftObject {
    ? articleCohorts;
    ? audioMultiplier;
    ? autofavoritedScore;
    ? bundleFreeMultiplier;
    ? bundlePaidMultiplier;
    ? clientScore;
    ? conversionCohorts;
    ? conversionMultiplier;
    ? conversionScore;
    ? dampenedStaticMultiplier;
    ? diversifiedScore;
    ? evergreenMultiplier;
    ? featuredMultiplier;
    ? firstPassScore;
    ? halfLifeCoefficient;
    ? nicheContentMultiplier;
    ? multiplierDampener;
    ? mutedVoteDampener;
    ? contentTriggerDampener;
    ? nicheContentScores;
    ? aggregateTriggerScoresByID;
    ? personalizedScore;
    ? qualitativeMultiplier;
    ? rawPersonalizedScore;
    ? readPenalty;
    ? seenPenalty;
    ? serverScore;
    ? rawServerScore;
    ? serverScoreCoefficient;
    ? sparseTagsPenalty;
    ? staticMultiplier;
    ? subscribedChannelScore;
    ? subscribedTopicScore;
    ? timeDecayPenalty;
    ? userFeedbackTimeDecayPenalty;
    ? timeDecayedScore;
    ? userCohortMemberships;
    ? tagWeightsByID;
    ? featureWeightsByID;
    ? titleEmbeddingScore;
    ? publisherFavorability;
}






@end


#endif