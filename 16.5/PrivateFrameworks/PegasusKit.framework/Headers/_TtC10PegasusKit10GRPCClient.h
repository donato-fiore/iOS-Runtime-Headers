// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC10PEGASUSKIT10GRPCCLIENT_H
#define _TTC10PEGASUSKIT10GRPCCLIENT_H

@class SwiftObject;



@interface _TtC10PegasusKit10GRPCClient : SwiftObject {
    ? activityMonitor;
    ? baseURLString;
    ? gatewayURLString;
    ? defaults;
    ? contextVendor;
    ? verboseDebugEnabled;
    ? jsonEncodingOptions;
    ? defaultGrpcTimeout;
    ? defaultMTUSizeInBytes;
    ? compressionEnabled;
    ? warmupInterval;
    ? warmupSpans;
    ? warmupQueue;
    ? debugParameters;
    ? feedbackEventStream;
    ? sharedSession;
}






@end


#endif