// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTCC16SIRIDIALOGENGINE18SIRIKITDEVICESTATE7BUILDER_H
#define _TTCC16SIRIDIALOGENGINE18SIRIKITDEVICESTATE7BUILDER_H

@class SwiftObject;



@interface _TtCC16SiriDialogEngine18SirikitDeviceState7Builder : SwiftObject {
    ? isHomePod;
    ? isWatch;
    ? isMac;
    ? isAppleTV;
    ? isPhone;
    ? isPad;
    ? isCarPlay;
    ? isCarDnD;
    ? isSidekick;
    ? isTextToSpeechEnabled;
    ? isLockedWithPasscode;
    ? isAuthenticated;
    ? isEyesFree;
    ? isVoiceTriggerEnabled;
    ? isVOX;
    ? isNetworkConnected;
    ? interactionType;
    ? inputOrigin;
    ? multiUserState;
    ? voiceGender;
    ? userAssignedDeviceName;
    ? deviceName;
    ? companionName;
    ? siriLocale;
    ? deviceRegionCode;
    ? restrictions;
}






@end


#endif