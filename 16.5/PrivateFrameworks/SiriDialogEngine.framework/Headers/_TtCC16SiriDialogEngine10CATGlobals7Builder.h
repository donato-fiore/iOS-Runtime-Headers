// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTCC16SIRIDIALOGENGINE10CATGLOBALS7BUILDER_H
#define _TTCC16SIRIDIALOGENGINE10CATGLOBALS7BUILDER_H

@class SwiftObject;



@interface _TtCC16SiriDialogEngine10CATGlobals7Builder : SwiftObject {
    ? siriLocale;
    ? deviceLocale;
    ? siriVoiceGender;
    ? isNetworkConnected;
    ? isMultiUserEnabled;
    ? requiresUserGrounding;
    ? randomSeed;
    ? currentTime;
    ? device;
    ? currentDevice;
    ? companionName;
    ? user;
    ? userSettings;
    ? customPronunciations;
    ? now;
    ? responseMode;
    ? unlockDevicePolicy;
    ? interactionId;
}






@end


#endif