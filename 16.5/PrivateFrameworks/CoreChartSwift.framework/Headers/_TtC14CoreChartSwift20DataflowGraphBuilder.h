// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC14CORECHARTSWIFT20DATAFLOWGRAPHBUILDER_H
#define _TTC14CORECHARTSWIFT20DATAFLOWGRAPHBUILDER_H

@class SwiftObject;



@interface _TtC14CoreChartSwift20DataflowGraphBuilder : SwiftObject {
    ? stateContext;
    ? spec;
    ? graph;
    ? signals;
    ? scaleSignals;
    ? tickValuesSignals;
    ? scenegraph;
    ? topLevelGestureHandler;
    ? padding;
    ? plotWidth;
    ? plotHeight;
    ? actualPlotWidth;
    ? actualPlotHeight;
    ? textMeasurements;
    ? environment;
    ? frameWidth;
    ? frameHeight;
    ? hasInteraction;
}






@end


#endif