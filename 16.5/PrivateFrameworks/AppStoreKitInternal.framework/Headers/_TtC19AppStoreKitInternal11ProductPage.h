// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC19APPSTOREKITINTERNAL11PRODUCTPAGE_H
#define _TTC19APPSTOREKITINTERNAL11PRODUCTPAGE_H

@class TtC19AppStoreKitInternal6Lockup;



@interface _TtC19AppStoreKitInternal11ProductPage : TtC19AppStoreKitInternal6Lockup {
    ? shareAction;
    ? developerAction;
    ? uberArtwork;
    ? uberArtworkForCompactDisplay;
    ? uberVideo;
    ? uberStyle;
    ? logoArtwork;
    ? backgroundIconArtwork;
    ? media;
    ? badges;
    ? isComplete;
    ? pageTopBackgroundColor;
    ? pageBottomBackgroundColor;
    ? hasDarkUserInterfaceStyle;
    ? headerMessage;
    ? isAskToBuyApproval;
    ? mediaSectionTitle;
    ? expandedOfferDetails;
    ? regularPriceFormatted;
    ? theme;
    ? externalVersionIdentifier;
    ? updateBuyParams;
    ? titleOfferDisplayProperties;
    ? banner;
    ? secondaryBanner;
    ? fullProductFetchedAction;
    ? appEventDetailPageAction;
    ? pageRefreshPolicy;
    ? alwaysAllowReviews;
    ? shelfMapping;
    ? purchasedOrdering;
    ? notPurchasedOrdering;
    ? pageMetrics;
    ? pageRenderEvent;
}






@end


#endif