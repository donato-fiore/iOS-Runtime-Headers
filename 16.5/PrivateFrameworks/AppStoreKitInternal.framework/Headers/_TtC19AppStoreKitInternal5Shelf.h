// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC19APPSTOREKITINTERNAL5SHELF_H
#define _TTC19APPSTOREKITINTERNAL5SHELF_H

@class SwiftObject;



@interface _TtC19AppStoreKitInternal5Shelf : SwiftObject {
    ? contentType;
    ? placeholderContentType;
    ? marker;
    ? items;
    ? prefetchableArtwork;
    ? url;
    ? mergeWhenFetched;
    ? batchGroup;
    ? seeAllAction;
    ? eyebrow;
    ? eyebrowArtwork;
    ? title;
    ? titleArtwork;
    ? subtitle;
    ? isHidden;
    ? contentsMetadata;
    ? nextPreferredContentRefreshDate;
    ? refreshUrl;
    ? pageChangeMetrics;
    ? isHorizontal;
    ? isPersonalized;
    ? background;
    ? rowsPerColumn;
    ? presentationHints;
    ? onRemoveModelEvents;
    ? onShouldRemoveModel;
    ? personalizationStyles;
    ? id;
    ? impressionMetrics;
    ? adIncidents;
}






@end


#endif