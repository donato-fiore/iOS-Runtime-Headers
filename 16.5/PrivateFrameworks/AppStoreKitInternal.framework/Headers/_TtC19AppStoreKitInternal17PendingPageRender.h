// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC19APPSTOREKITINTERNAL17PENDINGPAGERENDER_H
#define _TTC19APPSTOREKITINTERNAL17PENDINGPAGERENDER_H

@class SwiftObject;



@interface _TtC19AppStoreKitInternal17PendingPageRender : SwiftObject {
    ? metricsPipeline;
    ? lottery;
    ? gate;
    ? xpSessionDuration;
    ? xpSamplingPercentageUsers;
    ? hasEvent;
    ? event;
    ? hasRequestedTime;
    ? requestedTime;
    ? hasAppearTime;
    ? appearTime;
    ? disappearTime;
    ? resourceRequestStartTime;
    ? resourceRequestOnScreenEndTime;
    ? resourceRequestEndTime;
    ? userReadyTime;
    ? requests;
    ? isLayoutReady;
    ? hasViewModelPresentTime;
    ? rootViewModelPresentTime;
    ? isLowDataMode;
    ? assetLoaderIsIdle;
    ? assetLoader;
    ? hasInitialRequestEndTime;
    ? initialRequestEndTime;
    ? hasRequests;
    ? hasUserReadyTime;
    ? hasNetworkQualityReports;
    ? networkQualityReports;
    ? networkQualityInquiry;
    ? networkInquiry;
    ? errors;
    ? contextProvider;
    ? didPost;
}






@end


#endif