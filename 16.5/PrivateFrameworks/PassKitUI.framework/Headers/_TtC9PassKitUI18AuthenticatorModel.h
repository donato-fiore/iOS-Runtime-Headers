// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC9PASSKITUI18AUTHENTICATORMODEL_H
#define _TTC9PASSKITUI18AUTHENTICATORMODEL_H

@class SwiftObject;



@interface _TtC9PassKitUI18AuthenticatorModel : SwiftObject {
    ? _authenticating;
    ? _policy;
    ? _coachingState;
    ? _evaluationState;
    ? _bypassAuthenticator;
    ? _isInBioLockout;
    ? _disablePasscodeFallback;
    ? _footerViewType;
    ? _showEnterPasswordButton;
    ? _passcodeEntry;
    ? _disableInteractivity;
    ? _externalPasswordEntry;
    ? _glyph;
    ? _isAuthenticationDetached;
    ? _isSheetFullHeightOverrideEnabled;
    ? _shouldDisplaySheetFullHeight;
    ? glyphManager;
    ? userIntentStyle;
    ? hasPearl;
    ? physicalButtonEdge;
    ? _isEvaluationPaused;
    ? _isDetachedAuthenticationSheetPresented;
    ? physicalButtonHidden;
    ? context;
    ? externalizedContextCompletion;
    ? deferredFooterViewTypeUpdateToken;
}






@end


#endif