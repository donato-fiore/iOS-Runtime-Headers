// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTCO11MEDIACOREUI8BACKDROP23ROTATINGARTWORKRENDERER_H
#define _TTCO11MEDIACOREUI8BACKDROP23ROTATINGARTWORKRENDERER_H

@class SwiftObject;



@interface _TtCO11MediaCoreUI8Backdrop23RotatingArtworkRenderer : SwiftObject {
    ? colorPixelFormat;
    ? imageDownsample;
    ? blurRadius;
    ? outputSize;
    ? displayScale;
    ? context;
    ? vertices;
    ? indices;
    ? vertexBuffer;
    ? indexBuffer;
    ? pipelineState;
    ? renderPassDescriptor;
    ? texture;
    ? rotationDrawTexture;
    ? blurredTexture;
    ? blur;
}






@end


#endif