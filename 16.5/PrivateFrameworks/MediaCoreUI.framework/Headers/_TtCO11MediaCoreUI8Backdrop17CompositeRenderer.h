// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTCO11MEDIACOREUI8BACKDROP17COMPOSITERENDERER_H
#define _TTCO11MEDIACOREUI8BACKDROP17COMPOSITERENDERER_H


#import <Foundation/Foundation.h>


@interface _TtCO11MediaCoreUI8Backdrop17CompositeRenderer : NSObject {
    ? observer;
    ? state;
    ? framebufferPixelFormat;
    ? colorPixelFormat;
    ? isBehindLyrics;
    ? isPaused;
    ? crossfadeDuration;
    ? imageStorage;
    ? placeholderColor;
    ? context;
    ? environment;
    ? crossfadeTimingFunction;
    ? modeTimingFunction;
    ? warpTimingFunction;
    ? warpTimingSpeed;
    ? textureTransitionMix;
    ? pinchMix;
    ? uniforms;
    ? spectrumAnalysis;
    ? aspectRatio;
    ? renderers;
}




-(id)init;


@end


#endif