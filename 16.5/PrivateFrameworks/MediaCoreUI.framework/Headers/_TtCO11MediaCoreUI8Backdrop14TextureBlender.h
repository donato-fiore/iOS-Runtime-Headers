// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTCO11MEDIACOREUI8BACKDROP14TEXTUREBLENDER_H
#define _TTCO11MEDIACOREUI8BACKDROP14TEXTUREBLENDER_H

@class SwiftObject;



@interface _TtCO11MediaCoreUI8Backdrop14TextureBlender : SwiftObject {
    ? colorPixelFormat;
    ? outputSize;
    ? outputTexture;
    ? context;
    ? sourceTexture;
    ? blitToDest;
    ? destTexture;
    ? vertices;
    ? indices;
    ? vertexBuffer;
    ? indexBuffer;
    ? $__lazy_storage_$_pipelineDescriptor;
    ? pipelineState;
    ? renderPassDescriptor;
}






@end


#endif