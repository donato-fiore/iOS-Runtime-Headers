// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC9CHRONOKIT24LIVEWIDGETENTRYVIEWMODEL_H
#define _TTC9CHRONOKIT24LIVEWIDGETENTRYVIEWMODEL_H

@class SwiftObject;



@interface _TtC9ChronoKit24LiveWidgetEntryViewModel : SwiftObject {
    ? _allowsInternalTapTargets;
    ? _isInteractionDisabled;
    ? _clipToShape;
    ? _launchRequestHandler;
    ? _viewEntryDidChangePublisher;
    ? viewEntryDidChangePublisher;
    ? viewEntry;
    ? _rawEnvironmentValues;
    ? colorScheme;
    ? preferredColorScheme;
    ? isContentPaused;
    ? canAppearInSecureEnvironment;
    ? isForeground;
    ? _currentViewInLowLuminance;
    ? supportsLowLuminance;
    ? prefersUnredactedContentInLowLuminanceEnvironment;
    ? _overrideDateDidChangePublisher;
    ? overrideDateDidChangePublisher;
    ? overrideDate;
    ? tintParameters;
    ? inlineTextParameters;
    ? showsWidgetLabel;
    ? ignoreComplicationUserRedactionPreference;
    ? currentActionURL;
    ? canConsumeLaunchRequest;
    ? logIdentifier;
    ? logger;
    ? _liveSource;
    ? _placeholderSource;
    ? _viewableEntryCache;
    ? _invalidated;
    ? _hasLoadedViewOnce;
    ? _cancellable;
    ? _viewSecurityPolicy;
    ? _viewSecurityDataSource;
}






@end


#endif