// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC9CHRONOKIT11WIDGETENTRY_H
#define _TTC9CHRONOKIT11WIDGETENTRY_H

@class SwiftObject;



@interface _TtC9ChronoKit11WidgetEntry : SwiftObject {
    ? protectionType;
    ? _lock;
    ? key;
    ? _cachedViewableEntry;
    ? _configuredDate;
    ? _currentEntry;
    ? _shouldSilencePublisher;
    ? _environmentProvider;
    ? _content;
    ? _objectWillChangePublisher;
    ? objectWillChange;
    ? _contentDidChangePublisher;
    ? contentDidChangePublisher;
    ? _descriptorDidChangePublisher;
    ? descriptorDidChangePublisher;
    ? _descriptorSubscription;
    ? _descriptorProvider;
    ? descriptor;
}






@end


#endif