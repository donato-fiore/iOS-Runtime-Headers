// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC24REQUESTDISPATCHERBRIDGES14CONTEXTUPDATER_H
#define _TTC24REQUESTDISPATCHERBRIDGES14CONTEXTUPDATER_H

@class SwiftObject;



@interface _TtC24RequestDispatcherBridges14ContextUpdater : SwiftObject {
    ? sessionState;
    ? rraasClient;
    ? correctionsPlatformClient;
    ? serviceHelper;
    ? assistantId;
    ? requestId;
    ? contextDonator;
    ? keysManager;
    ? requestQueue;
    ? responseText;
    ? instrumentationUtil;
    ? finalNativeFlowContextCommand;
    ? finalActiveTasks;
    ? finalExecutedTasks;
    ? userId;
    ? domainHints;
}






@end


#endif