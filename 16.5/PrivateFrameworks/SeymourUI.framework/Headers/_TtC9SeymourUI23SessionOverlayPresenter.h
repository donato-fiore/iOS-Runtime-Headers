// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC9SEYMOURUI23SESSIONOVERLAYPRESENTER_H
#define _TTC9SEYMOURUI23SESSIONOVERLAYPRESENTER_H

@class SwiftObject;



@interface _TtC9SeymourUI23SessionOverlayPresenter : SwiftObject {
    ? display;
    ? externalOverlayDisplay;
    ? subscriptionToken;
    ? eventHub;
    ? burnBarPresenter;
    ? activeEnergyPresenter;
    ? distancePresenter;
    ? heartRatePresenter;
    ? inclinePresenter;
    ? intensityPresenter;
    ? multiUserCelebrationPresenter;
    ? progressPresenter;
    ? ringsPresenter;
    ? rpmPresenter;
    ? spmPresenter;
    ? sessionOrigin;
    ? timerPresenter;
    ? trackPresenter;
    ? stringBuilder;
    ? externalOverlayStringBuilder;
    ? metricBuilder;
    ? platform;
    ? timerProvider;
    ? popupEventDismissalTimer;
    ? metricMinimizerTimer;
    ? displayPreferences;
    ? activityTypeBehavior;
    ? activityRings;
    ? workoutDeviceConnection;
    ? supportedElements;
    ? currentElements;
}






@end


#endif