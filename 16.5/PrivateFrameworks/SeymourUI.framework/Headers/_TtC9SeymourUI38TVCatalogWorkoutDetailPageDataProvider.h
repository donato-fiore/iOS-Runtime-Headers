// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC9SEYMOURUI38TVCATALOGWORKOUTDETAILPAGEDATAPROVIDER_H
#define _TTC9SEYMOURUI38TVCATALOGWORKOUTDETAILPAGEDATAPROVIDER_H

@class SwiftObject;



@interface _TtC9SeymourUI38TVCatalogWorkoutDetailPageDataProvider : SwiftObject {
    ? dependencies;
    ? marketingItemProvider;
    ? bookmarkClient;
    ? catalogClient;
    ? remoteBrowsingSource;
    ? subscriptionCache;
    ? configurationClient;
    ? workoutIdentifier;
    ? eventHub;
    ? subscriptionToken;
    ? page;
    ? workoutDetail;
    ? marketingOffer;
    ? marketingPlacement;
    ? metricPageName;
    ? compositeHeaderShelf;
    ? musicTracksShelf;
    ? relatedWorkoutsShelf;
    ? summaryShelf;
    ? mediaTagStringBuilder;
    ? metadataShelf;
    ? lockup;
    ? backgroundArtworkDelegate;
    ? startActivityType;
    ? shouldPublishPageEventWhenAvailable;
}






@end


#endif