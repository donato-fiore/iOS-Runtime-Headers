// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC9SEYMOURUI33TVCATALOGWORKOUTDETAILHEADERSHELF_H
#define _TTC9SEYMOURUI33TVCATALOGWORKOUTDETAILHEADERSHELF_H

@class SwiftObject;



@interface _TtC9SeymourUI33TVCatalogWorkoutDetailHeaderShelf : SwiftObject {
    ? delegate;
    ? identifier;
    ? archivedSessionClient;
    ? bag;
    ? bookmarkClient;
    ? catalogClient;
    ? dependencies;
    ? eventHub;
    ? remoteBrowsingSource;
    ? storefrontLocalizer;
    ? mediaTagStringBuilder;
    ? subscriptionToken;
    ? multiUserEligibilityObserver;
    ? row;
    ? workoutDetail;
    ? bookmarks;
    ? resumableSession;
    ? calendar;
    ? inProgressButtonActions;
    ? multiUserEligibilitySubscription;
    ? marketingOffer;
    ? config;
    ? serviceSubscription;
    ? matchingDeviceSubscriberAccount;
    ? deviceSubscriberAccount;
    ? deviceCloudAccount;
    ? accountHasSupportedDevice;
    ? accountHasWatch;
}






@end


#endif