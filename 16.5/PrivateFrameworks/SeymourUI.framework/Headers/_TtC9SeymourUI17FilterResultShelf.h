// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC9SEYMOURUI17FILTERRESULTSHELF_H
#define _TTC9SEYMOURUI17FILTERRESULTSHELF_H

@class SwiftObject;



@interface _TtC9SeymourUI17FilterResultShelf : SwiftObject {
    ? delegate;
    ? identifier;
    ? catalogClient;
    ? eventHub;
    ? subscriptionToken;
    ? rootProperty;
    ? lazyLockupFetcher;
    ? platform;
    ? activeSortMode;
    ? bodyFocusProperty;
    ? durationProperty;
    ? equipmentProperty;
    ? genreProperty;
    ? modalityProperty;
    ? skillLevelProperty;
    ? themeProperty;
    ? trainerProperty;
    ? rootFilterResult;
    ? rootFilterName;
    ? filterResult;
    ? filterOptions;
    ? filter;
    ? row;
    ? displayState;
    ? rootModality;
}






@end


#endif