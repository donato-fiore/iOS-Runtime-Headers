// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC37ICLOUDSUBSCRIPTIONOPTIMIZERLIGHTHOUSE14SHADOWMODEMAIN_H
#define _TTC37ICLOUDSUBSCRIPTIONOPTIMIZERLIGHTHOUSE14SHADOWMODEMAIN_H

@class SwiftObject;



@interface _TtC37iCloudSubscriptionOptimizerLighthouse14ShadowModeMain : SwiftObject {
    ? trialSet;
    ? recipePath;
    ? modelPaths;
    ? biomeManager;
    ? fileManager;
    ? skipPublishMetrics;
    ? numOffsets;
    ? notifStartDaysUntilNow;
    ? notifEndDaysUntilNow;
    ? labelDurationInDays;
    ? offsetTimeInterval;
    ? labelingSource;
    ? percentiles;
    ? dummyData;
    ? minsUntilOpen;
}






@end


#endif