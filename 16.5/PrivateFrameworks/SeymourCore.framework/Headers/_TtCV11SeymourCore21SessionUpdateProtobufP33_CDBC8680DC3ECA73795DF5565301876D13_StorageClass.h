// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTCV11SEYMOURCORE21SESSIONUPDATEPROTOBUFP33_CDBC8680DC3ECA73795DF5565301876D13_STORAGECLASS_H
#define _TTCV11SEYMOURCORE21SESSIONUPDATEPROTOBUFP33_CDBC8680DC3ECA73795DF5565301876D13_STORAGECLASS_H

@class SwiftObject;



@interface _TtCV11SeymourCore21SessionUpdateProtobufP33_CDBC8680DC3ECA73795DF5565301876D13_StorageClass : SwiftObject {
    ? _updateIdentifier;
    ? _sessionIdentifier;
    ? _activityRings;
    ? _countdownIntent;
    ? _countdown;
    ? _energyMetric;
    ? _heartRateMetric;
    ? _playbackIntent;
    ? _playback;
    ? _termination;
    ? _workout;
    ? _scoreMetric;
    ? _userMassMetric;
    ? _distanceMetric;
    ? _workoutDeviceConnection;
    ? _gymKitConnection;
    ? _displayPreferences;
    ? _unitPreferences;
    ? _watchConnectionRejection;
    ? _remoteDisplayConnection;
    ? _paceMetric;
    ? _machTimestamp;
    ? _alternateMetric;
    ? _alternate;
    ? _multiUserSession;
    ? _multiUserSessionIdentities;
}






@end


#endif