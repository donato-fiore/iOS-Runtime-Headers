// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC6CHARTS20DATAFLOWGRAPHBUILDER_H
#define _TTC6CHARTS20DATAFLOWGRAPHBUILDER_H

@class SwiftObject;



@interface _TtC6Charts20DataflowGraphBuilder : SwiftObject {
    ? stateContext;
    ? spec;
    ? graph;
    ? signals;
    ? scaleSignals;
    ? idContext;
    ? plotArea;
    ? axes;
    ? legendContext;
    ? accessibilityElements;
    ? accessibilityAudioGraph;
    ? padding;
    ? safeMargin;
    ? plotWidth;
    ? plotHeight;
    ? coordinateSystem;
    ? scaleAdjustments;
    ? textMeasurements;
    ? environment;
    ? frameWidth;
    ? frameHeight;
    ? hasInteraction;
}






@end


#endif