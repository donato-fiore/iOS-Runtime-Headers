// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC6CHARTS25CHARTCONTENTRENDERCONTEXT_H
#define _TTC6CHARTS25CHARTCONTENTRENDERCONTEXT_H

@class SwiftObject;



@interface _TtC6Charts25ChartContentRenderContext : SwiftObject {
    ? idContext;
    ? xRange;
    ? yRange;
    ? coordinateSystem;
    ? rasterizationScale;
    ? environment;
    ? textMeasurements;
    ? xScale;
    ? yScale;
    ? xOffsetScale;
    ? yOffsetScale;
    ? foregroundStyleScale;
    ? symbolScale;
    ? symbolSizeScale;
    ? lineStyleScale;
    ? stackContext;
    ? subPositionContext;
    ? seriesContext;
    ? markInsets;
    ? bins;
}






@end


#endif