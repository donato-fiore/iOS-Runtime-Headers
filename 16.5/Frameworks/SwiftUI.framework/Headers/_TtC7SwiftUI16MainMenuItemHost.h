// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC7SWIFTUI16MAINMENUITEMHOST_H
#define _TTC7SWIFTUI16MAINMENUITEMHOST_H

@class SwiftObject;



@interface _TtC7SwiftUI16MainMenuItemHost : SwiftObject {
    ? viewGraph;
    ? eventBindingManager;
    ? propertiesNeedingUpdate;
    ? renderingPhase;
    ? accessibilityVersion;
    ? currentTimestamp;
    ? externalUpdateCount;
    ? mainMenuItem;
    ? environment;
    ? focusedValues;
    ? focusStore;
    ? delegate;
    ? currentAccessibilityFocusStore;
}






@end


#endif