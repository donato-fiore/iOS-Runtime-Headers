// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC7SWIFTUI9VIEWGRAPH_H
#define _TTC7SWIFTUI9VIEWGRAPH_H

@class TtC7SwiftUI9GraphHost;



@interface _TtC7SwiftUI9ViewGraph : TtC7SwiftUI9GraphHost {
    ? rootViewType;
    ? makeRootView;
    ? delegate;
    ? centersRootView;
    ? rootView;
    ? _rootTransform;
    ? _zeroPoint;
    ? _emptyViewResponders;
    ? _emptyPlatformItemList;
    ? _proposedSize;
    ? _safeAreaInsets;
    ? _rootGeometry;
    ? _position;
    ? _dimensions;
    ? _gestureTime;
    ? _gestureEvents;
    ? _inheritedPhase;
    ? _gestureResetSeed;
    ? _failedPhase;
    ? _rootPhase;
    ? eventSubgraph;
    ? _defaultLayoutComputer;
    ? _focusedItem;
    ? _focusedValues;
    ? _focusStore;
    ? _accessibilityFocusStore;
    ? _accessibilityFocus;
    ? _rootResponders;
    ? _rootAccessibilityNodes;
    ? _rootLayoutComputer;
    ? _rootDisplayList;
    ? _rootPlatformItemList;
    ? cachedSizeThatFits;
    ? sizeThatFitsObserver;
    ? requestedOutputs;
    ? disabledOutputs;
    ? mainUpdates;
    ? needsFocusUpdate;
    ? nextUpdate;
    ? _preferenceBridge;
    ? bridgedPreferences;
    ? accessibilityRelationshipScope;
}






@end


#endif