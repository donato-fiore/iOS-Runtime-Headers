// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC8ROOMPLAN15SETTINGSMANAGER_H
#define _TTC8ROOMPLAN15SETTINGSMANAGER_H

@class SwiftObject;



@interface _TtC8RoomPlan15SettingsManager : SwiftObject {
    ? delegate;
    ? useDeterministicFPSMode;
    ? upscaleDepth;
    ? isOcclusionEnabled;
    ? lineFeatheringMaxDistance;
    ? objectRenderingMode;
    ? hideSelectiveObjects;
    ? showOfflineChairs;
    ? sinkOverlapPercentage;
    ? wallAlignInWorld;
    ? animateIncompleteState;
    ? lineFadeInDuration;
    ? lineFadeOutDuration;
    ? wallAnimationDuration;
    ? objectAnimationDuration;
    ? openingAnimationDuration;
    ? useAlphaOcclusion;
    ? mediumConfidenceOcclusionFactor;
    ? lowConfidenceOcclusionFactor;
    ? occlusionTolerance;
    ? occlusionAlphaMinValue;
    ? cameraDimmingFactor;
    ? renderBloom;
    ? bloomSigma;
    ? $__lazy_storage_$_isBloomRenderedIn2Passes;
    ? $__lazy_storage_$_arViewResolution;
    ? arViewPixelFormat;
    ? isVideoCompositingEnabled;
    ? showStatistics;
    ? resolutionVelocityThreshold;
    ? framerateVelocityThreshold;
    ? arViewFramerateLowerBound;
    ? arKitFramerateLowerBound;
    ? arViewResolutionLowerBound;
    ? useDynamicArViewFormat;
    ? $__lazy_storage_$_arViewFramerate;
    ? $__lazy_storage_$_arKitFramerate;
    ? isSmoothedSceneDepthEnabled;
    ? isDebugIndividualUpdatesEnabled;
    ? isARQATracingEnabled;
    ? isARFrameLogEnabled;
    ? isFramerateLogEnabled;
    ? isLiveDumpEnabled;
    ? isOURGBRefinementEnabled;
    ? renderWalls;
    ? renderObjects;
    ? renderOpenings;
    ? renderARMarkers;
    ? lineWidth;
    ? isPerimeterDebugVisualizationEnabled;
    ? isDebugVisualizationEnabled;
    ? isModelEnabled;
    ? isLightingDebugEnabled;
    ? dollHouseEmissive;
    ? dollHouseMetallic;
    ? dollHouseRoughness;
    ? dollHouseAmbientLightIntensity;
    ? dollHouseDirectionalLightDirection;
    ? dollHouseDirectionalLightIntensity;
    ? dollHouseGradient;
    ? dollHouseHemi;
    ? dollHouseShadowBias;
    ? dollHouseShadowDistance;
    ? isPerimeterLogDumpingEnabled;
}






@end


#endif