// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC8ROOMPLAN16DEVICECONTROLLER_H
#define _TTC8ROOMPLAN16DEVICECONTROLLER_H

@class SwiftObject;



@interface _TtC8RoomPlan16DeviceController : SwiftObject {
    ? deviceMaskLayer;
    ? aspectRatio;
    ? holeLayer;
    ? shineLayer;
    ? shineInnerLayer;
    ? deviceLayer;
    ? isIpad;
    ? bounds;
    ? position;
    ? transform;
    ? opacity;
    ? kDeviceGlyphHeight;
}






@end


#endif