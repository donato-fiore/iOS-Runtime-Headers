// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC8ROOMPLAN17TRAJECTORYMANAGER_H
#define _TTC8ROOMPLAN17TRAJECTORYMANAGER_H

@class SwiftObject;



@interface _TtC8RoomPlan17TrajectoryManager : SwiftObject {
    ? state;
    ? lastDeviceOrientation;
    ? lastDirection;
    ? alignmentBuffer;
    ? directionBuffer;
    ? hitTestPositions;
    ? smoothedHitTestPositions;
    ? allowedRaycast;
    ? bufferCount;
    ? up;
    ? down;
    ? right;
    ? left;
    ? forward;
    ? backwards;
    ? $__lazy_storage_$_bufferCountVector;
    ? currentAnchor;
}






@end


#endif