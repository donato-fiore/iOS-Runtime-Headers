// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 16.5.0 | SDK: 16.5.0


#ifndef _TTC8ROOMPLAN13ENTITYMANAGER_H
#define _TTC8ROOMPLAN13ENTITYMANAGER_H

@class SwiftObject;



@interface _TtC8RoomPlan13EntityManager : SwiftObject {
    ? delegate;
    ? materialFactory;
    ? parentEntity;
    ? scanContainer;
    ? scanEntityType;
    ? wallEntities;
    ? objectEntities;
    ? openingEntities;
    ? enableDebugVisualization;
    ? contentScaleFactor;
    ? edgeEntityFactory;
    ? defaultPointLineWidth;
    ? lineWidth;
}






@end


#endif