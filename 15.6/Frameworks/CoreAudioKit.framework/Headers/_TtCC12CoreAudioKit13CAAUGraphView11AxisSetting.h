// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 15.6.0 | SDK: 15.6.0


#ifndef _TTCC12COREAUDIOKIT13CAAUGRAPHVIEW11AXISSETTING_H
#define _TTCC12COREAUDIOKIT13CAAUGRAPHVIEW11AXISSETTING_H


#import <Foundation/Foundation.h>


@interface _TtCC12CoreAudioKit13CAAUGraphView11AxisSetting : NSObject {
    ? name;
    ? majorLine;
    ? minorLine;
    ? centerLine;
    ? gridLineCount;
    ? minAllowed;
    ? maxAllowed;
    ? min;
    ? max;
    ? center;
    ? logBase;
    ? clipDisplayToMax;
    ? unitsLabel;
    ? abbreviateValues;
    ? drawFirstAndLastKOnly;
    ? suppressInitialUnit;
    ? showActiveArea;
    ? suppressLabels;
    ? extendToZero;
    ? extendHorizontally;
}




-(id)init;


@end


#endif