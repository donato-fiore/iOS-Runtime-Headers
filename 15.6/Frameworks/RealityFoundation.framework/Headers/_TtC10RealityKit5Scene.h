// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 15.6.0 | SDK: 15.6.0


#ifndef _TTC10REALITYKIT5SCENE_H
#define _TTC10REALITYKIT5SCENE_H

@class SwiftObject;



@interface _TtC10RealityKit5Scene : SwiftObject {
    ? coreScene;
    ? realityAssetCollisionSubscription;
    ? session;
    ? __audioListener;
    ? internalAudioListener;
    ? defaultCamera;
    ? audioMixer;
    ? activeCamera;
    ? entities;
    ? $__lazy_storage_$_eventService;
    ? updateEventCancellable;
    ? $__lazy_storage_$___interactionService;
    ? synchronizationService;
}






@end


#endif