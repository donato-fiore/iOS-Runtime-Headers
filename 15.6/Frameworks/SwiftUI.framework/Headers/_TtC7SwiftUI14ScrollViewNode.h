// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 15.6.0 | SDK: 15.6.0


#ifndef _TTC7SWIFTUI14SCROLLVIEWNODE_H
#define _TTC7SWIFTUI14SCROLLVIEWNODE_H

@class SwiftObject;



@interface _TtC7SwiftUI14ScrollViewNode : SwiftObject {
    ? host;
    ? attribute;
    ? uniqueId;
    ? modelOffset;
    ? presentationOffset;
    ? behavior;
    ? isInitialized;
    ? resetSeed;
    ? config;
    ? contentSize;
    ? containerSize;
    ? decelerationTarget;
    ? container;
    ? topScrollIndicatorFollowsContentOffset;
    ? pixelLength;
    ? propertySeed;
}






@end


#endif