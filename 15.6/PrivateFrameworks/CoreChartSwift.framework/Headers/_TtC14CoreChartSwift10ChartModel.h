// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 15.6.0 | SDK: 15.6.0


#ifndef _TTC14CORECHARTSWIFT10CHARTMODEL_H
#define _TTC14CORECHARTSWIFT10CHARTMODEL_H

@class SwiftObject;



@interface _TtC14CoreChartSwift10ChartModel : SwiftObject {
    ? chartInternal;
    ? chartInternalVersion;
    ? layoutHelper;
    ? textMeasurements;
    ? currentPartialSpec;
    ? currentPartialSpecVersion;
    ? graph;
    ? persistentComponents;
    ? signals;
    ? plotWidthSignal;
    ? plotHeightSignal;
    ? environmentSignals;
    ? textMeasurementsSignal;
    ? paddingSignal;
    ? scenegraph;
    ? origin;
    ? safeMargin;
    ? topLevelGestureHandler;
    ? hasInteraction;
    ? mutex;
}






@end


#endif