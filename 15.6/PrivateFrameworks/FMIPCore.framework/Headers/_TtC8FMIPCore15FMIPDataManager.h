// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 15.6.0 | SDK: 15.6.0


#ifndef _TTC8FMIPCORE15FMIPDATAMANAGER_H
#define _TTC8FMIPCORE15FMIPDATAMANAGER_H

@class SwiftObject;



@interface _TtC8FMIPCore15FMIPDataManager : SwiftObject {
    ? enabledSubsystems;
    ? isSnapshotMode;
    ? delegate;
    ? identifier;
    ? isCacheDataValid;
    ? dataQueue;
    ? devices;
    ? owner;
    ? familyMembers;
    ? crowdSourcedOriginalLocations;
    ? crowdSourcedLocations;
    ? crowdSourcedLocating;
    ? safeLocations;
    ? safeLocationsMapping;
    ? separationStates;
    ? separationSupported;
    ? canBeLeashedByHost;
    ? items;
    ? itemGroups;
    ? unknownItems;
    ? itemsCache;
    ? itemGroupsCache;
    ? devicesCache;
    ? familyCache;
    ? ownerCache;
    ? separationMonitoringCache;
    ? preferredLocale;
    ? geocodingCache;
}






@end


#endif