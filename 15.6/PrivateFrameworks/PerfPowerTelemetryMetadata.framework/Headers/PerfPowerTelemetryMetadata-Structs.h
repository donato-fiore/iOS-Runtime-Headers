typedef struct _NSZone {
} // Error Processing Struct Fields

typedef struct ? {
    BOOL version;
    BOOL datatype;
    BOOL directionality;
    BOOL enabledPopulation;
    BOOL mode;
    BOOL storage;
    BOOL timeToLive;
} ?;

