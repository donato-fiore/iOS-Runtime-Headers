// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 15.6.0 | SDK: 15.6.0


#ifndef _TTC19APPSTOREKITINTERNAL6LOCKUP_H
#define _TTC19APPSTOREKITINTERNAL6LOCKUP_H

@class TtC19AppStoreKitInternal5Model;



@interface _TtC19AppStoreKitInternal6Lockup : TtC19AppStoreKitInternal5Model {
    ? adamId;
    ? bundleId;
    ? icon;
    ? isEditorsChoice;
    ? ordinal;
    ? heading;
    ? title;
    ? subtitle;
    ? developerTagline;
    ? rating;
    ? ratingCount;
    ? editorialTagline;
    ? editorialDescription;
    ? shortEditorialDescription;
    ? buttonAction;
    ? offerDisplayProperties;
    ? ageRating;
    ? clickAction;
    ? crossLinkTitle;
    ? crossLinkSubtitle;
    ? children;
    ? tertiaryTitle;
    ? tertiaryTitleAction;
    ? contextMenuData;
    ? includeBetaApps;
    ? impressionMetrics;
    ? shelfBackground;
    ? searchAd;
    ? itemBackground;
    ? decorations;
    ? flowPreviewActionsConfiguration;
}






@end


#endif