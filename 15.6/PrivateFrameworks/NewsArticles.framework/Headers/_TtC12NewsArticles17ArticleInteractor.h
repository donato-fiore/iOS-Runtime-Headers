// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 15.6.0 | SDK: 15.6.0


#ifndef _TTC12NEWSARTICLES17ARTICLEINTERACTOR_H
#define _TTC12NEWSARTICLES17ARTICLEINTERACTOR_H

@class SwiftObject;



@interface _TtC12NewsArticles17ArticleInteractor : SwiftObject {
    ? delegate;
    ? coordinator;
    ? article;
    ? articleAccess;
    ? dataManager;
    ? readingListObserver;
    ? paywallFactory;
    ? previewManager;
    ? contextBuilder;
    ? eventManager;
    ? offerManager;
    ? paywallTypeProvider;
    ? articlePurchaseOffersProvider;
    ? loadingIndicatorProvider;
    ? articleLoadingStateMachine;
    ? errorMessageFactory;
    ? loggingIdentifier;
    ? articleReadingHistoryRecorder;
}






@end


#endif