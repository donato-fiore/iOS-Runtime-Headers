// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 15.6.0 | SDK: 15.6.0


#ifndef _TTC15SEYMOURSERVICES13SESSIONSYSTEM_H
#define _TTC15SEYMOURSERVICES13SESSIONSYSTEM_H

@class SwiftObject;



@interface _TtC15SeymourServices13SessionSystem : SwiftObject {
    ? analyticsReporter;
    ? appForegrounder;
    ? appRequirementCoordinator;
    ? appTerminationMonitor;
    ? biomeCoordinator;
    ? carouselSessionCoordinator;
    ? completionCoordinator;
    ? connectionBrokerProvider;
    ? connectionPool;
    ? distributedSessionCoordinator;
    ? handshakeCoordinator;
    ? eventHub;
    ? heartbeatCoordinator;
    ? idleMonitor;
    ? integrityMonitor;
    ? lock;
    ? remoteDisplayCoordinator;
    ? subscriptionToken;
    ? transactionCoordinator;
    ? xpcBroadcastConnection;
    ? state;
}






@end


#endif