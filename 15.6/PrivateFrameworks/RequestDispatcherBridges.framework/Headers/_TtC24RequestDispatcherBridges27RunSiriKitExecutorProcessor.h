// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 15.6.0 | SDK: 15.6.0


#ifndef _TTC24REQUESTDISPATCHERBRIDGES27RUNSIRIKITEXECUTORPROCESSOR_H
#define _TTC24REQUESTDISPATCHERBRIDGES27RUNSIRIKITEXECUTORPROCESSOR_H

@class SwiftObject;



@interface _TtC24RequestDispatcherBridges27RunSiriKitExecutorProcessor : SwiftObject {
    ? command;
    ? executionContext;
    ? reply;
    ? executionOutputSubmitter;
    ? executionClient;
    ? contextUpdater;
    ? requestId;
    ? assistantId;
    ? source;
    ? requestDispatcherServiceHelper;
    ? instrumentationUtil;
    ? requestType;
    ? requestQueue;
    ? outputCounter;
    ? loggedStartOfCall;
    ? resultCandidateId;
    ? requestFinished;
    ? flowPluginInfo;
}






@end


#endif