// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 15.6.0 | SDK: 15.6.0


#ifndef _TTC8NEWSFEED17DEBUGFORMATEDITOR_H
#define _TTC8NEWSFEED17DEBUGFORMATEDITOR_H

@class SwiftObject;



@interface _TtC8NewsFeed17DebugFormatEditor : SwiftObject {
    ? key;
    ? factory;
    ? fontManager;
    ? featureAvailablity;
    ? appConfigManager;
    ? imageCache;
    ? commandCenter;
    ? commandSource;
    ? subscriptionManager;
    ? userInfo;
    ? _bindings;
    ? _statusMessage;
    ? _canvasDevice;
    ? _layoutOrder;
    ? _showMissingOptionBindings;
    ? _consoleTrackingNow;
    ? _consoleAnimating;
    ? _consoleDisplay;
    ? _consoleFilterTerm;
    ? _wildcardSlotFallback;
    ? _layoutIdentifier;
    ? _backgroundLayoutIdentifier;
    ? _groupIdentifier;
    ? _backgroundGroupIdentifier;
    ? _bindingIdentifier;
    ? _allowSelection;
    ? _alwaysBounceVertical;
    ? _alwaysBounceHorizontal;
    ? _showColumns;
    ? _showLayoutMargins;
    ? _showLayoutPaneOnSnapshot;
    ? _userInterfaceStyle;
    ? _showRulers;
    ? _inventorySearchTerm;
    ? _history;
    ? _isCompiling;
    ? _isSnapshotDebugging;
    ? _compilerResult;
    ? _themeIdentifiers;
    ? _pinnedIdentifiers;
    ? _hiddenInventoryIdentifiers;
    ? _uploadTarget;
    ? _uploadEnvironment;
    ? _uploadUsername;
    ? _uploadPassword;
    ? _uploadDiffingEnabled;
    ? _featureAppleNews;
    ? _featureAppleNewsPrivateData;
    ? _featureAppleNewsAudio;
    ? _featureAppleNewsMagazines;
    ? _videoInFeed;
    ? _numberOfTestItems;
    ? _runTestsAutomatically;
    ? _testPreviewScale;
    ? _testScope;
    ? _testRun;
    ? _testSubject;
    ? _showLayoutFirstPass;
    ? _showLayoutSecondPass;
    ? _showLayoutBoxes;
    ? _showLayoutFrames;
    ? _showLayoutSpaces;
    ? _showLayoutEmbeds;
    ? _hideContentView;
    ? _metricsEnabled;
    ? _metricsOrderBy;
    ? _location;
    ? _historyGraphRelativeTimeInMilliseconds;
    ? _validiatedGroupsOnly;
    ? _compileSelectedPackagesOnly;
    ? _themeIdentifier;
    ? _pinnedIdentifier;
    ? _hiddenInventoryIdentifier;
    ? _feedKind;
    ? _overrideFeedKind;
    ? _compilerSettings;
    ? compiler;
    ? $__lazy_storage_$_fonts;
}






@end


#endif