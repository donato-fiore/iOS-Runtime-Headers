// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 15.6.0 | SDK: 15.6.0


#ifndef _TTC8NEWSFEED16FORMATBUTTONNODE_H
#define _TTC8NEWSFEED16FORMATBUTTONNODE_H

@class SwiftObject;



@interface _TtC8NewsFeed16FormatButtonNode : SwiftObject {
    ? _identifier;
    ? _size;
    ? _adjustments;
    ? _zIndex;
    ? _visibility;
    ? _isUserInteractionEnabled;
    ? _children;
    ? _stateMasks;
    ? action;
    ? actionName;
    ? longPressAction;
    ? longPressActionName;
    ? accessibilityRole;
    ? animation;
    ? _selectors;
    ? name;
}






@end


#endif