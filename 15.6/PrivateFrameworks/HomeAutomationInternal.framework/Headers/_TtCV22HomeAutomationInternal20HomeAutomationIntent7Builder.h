// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 15.6.0 | SDK: 15.6.0


#ifndef _TTCV22HOMEAUTOMATIONINTERNAL20HOMEAUTOMATIONINTENT7BUILDER_H
#define _TTCV22HOMEAUTOMATIONINTERNAL20HOMEAUTOMATIONINTENT7BUILDER_H

@class SwiftObject;



@interface _TtCV22HomeAutomationInternal20HomeAutomationIntent7Builder : SwiftObject {
    ? capabilityAttribute;
    ? confirmation;
    ? dateTime;
    ? dateAttribute;
    ? delta;
    ? deltaQuantifier;
    ? deltaRole;
    ? deviceQuantifiers;
    ? event;
    ? home;
    ? hues;
    ? limit;
    ? misc;
    ? noun;
    ? placeHint;
    ? tagSets;
    ? rooms;
    ? scenes;
    ? services;
    ? accessoryTypes;
    ? serviceNames;
    ? serviceGroup;
    ? accessoryNames;
    ? fromEntities;
    ? toEntities;
    ? states;
    ? units;
    ? verb;
    ? questionType;
    ? phrases;
    ? zones;
    ? trigger;
    ? fallbackWebSearchQuery;
    ? numericValues;
    ? allWordsMatched;
    ? referenceType;
    ? serviceTypeSpans;
    ? accessoryTypeSpans;
    ? serviceNameSpans;
    ? accessoryNameSpans;
    ? groupNameSpans;
    ? resolvedFiltersInReference;
    ? flags;
}






@end


#endif