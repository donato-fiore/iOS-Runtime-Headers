// Headers generated with ktool v2.0.0
// https://github.com/cxnder/ktool | pip3 install k2l
// Platform: IOS | Minimum OS: 15.6.0 | SDK: 15.6.0


#ifndef _TTC9SEYMOURUI21FILTEROPTIONLISTSHELF_H
#define _TTC9SEYMOURUI21FILTEROPTIONLISTSHELF_H

@class SwiftObject;



@interface _TtC9SeymourUI21FilterOptionListShelf : SwiftObject {
    ? delegate;
    ? identifier;
    ? catalogClient;
    ? eventHub;
    ? storefrontLocalizer;
    ? subscriptionToken;
    ? title;
    ? rootProperty;
    ? rootFilterResult;
    ? rootModality;
    ? filterOptions;
    ? row;
    ? filter;
    ? filterResult;
    ? initialFilterResult;
    ? bodyFocusProperty;
    ? durationProperty;
    ? equipmentProperty;
    ? genreProperty;
    ? modalityProperty;
    ? skillLevelProperty;
    ? themeProperty;
    ? trainerProperty;
    ? options;
}






@end


#endif